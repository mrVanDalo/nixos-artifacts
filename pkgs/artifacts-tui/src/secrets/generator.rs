//! Secret generation helpers (stub)

pub fn execute_generator_script(
    _script: &str,
    _prompts_dir: &str,
    _out_dir: &str,
) -> std::io::Result<()> {
    // TODO: run bubblewrap and execute script with env vars
    Ok(())
}
