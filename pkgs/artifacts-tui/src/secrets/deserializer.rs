//! Deserialization helpers (stub)

#[allow(dead_code)]
pub fn execute_deserialize_script(
    _script: &str,
    _out_dir: &str,
    _machine: &str,
    _artifact: &str,
) -> std::io::Result<()> {
    // TODO: run bubblewrap and execute deserializer
    Ok(())
}
