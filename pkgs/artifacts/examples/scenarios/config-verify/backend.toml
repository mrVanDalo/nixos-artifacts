# Test scenario that uses the test-config-verify backend
# This backend validates that $targets is properly set with the expected
# context, target list, and per-target config (single + shared cases).

include = ["../../backends/test-config-verify/backend.toml"]