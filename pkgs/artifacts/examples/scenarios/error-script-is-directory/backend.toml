# check.sh is a directory, not a file - should fail with "is not a file" error
[test.nixos]
check = "./check.sh"
serialize = "./serialize.sh"

[test.home]
check = "./check.sh"
serialize = "./serialize.sh"