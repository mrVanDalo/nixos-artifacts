---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/channels.rs
  - Cargo.toml
autonomous: true

must_haves:
  truths:
    - Channel types exist for foreground/background communication
    - Unbounded mpsc channels are created
    - EffectCommand and EffectResult enums contain all required variants
    - Each message includes artifact_index for dispatch context
  artifacts:
    - path: src/tui/channels.rs
      provides: "Channel message type definitions"
      exports: ["EffectCommand", "EffectResult"]
      min_lines: 80
    - path: Cargo.toml
      provides: "tokio dependency confirmed"
      contains: "tokio"
  key_links:
    - from: "EffectCommand"
      to: "Effect variants"
      via: "one-to-one mapping"
      pattern: "CheckSerialization -> CheckSerialization"
---

<objective>
Create the foundational channel communication system enabling async foreground/background communication.

Purpose: Establish the message-passing infrastructure that will power all effect
execution without blocking the TUI. Output: Channel message types in
src/tui/channels.rs, tokio confirmed in Cargo.toml
</objective>

<execution_context> @./.opencode/agents/gsd-executor.md </execution_context>

<context>
@../../pkgs/artifacts/src/app/effect.rs
@../../pkgs/artifacts/src/app/message.rs
@../../pkgs/artifacts/Cargo.toml

## Decisions (from CONTEXT.md - LOCKED)

### Channel Design

- **Message types:** Separate per-effect variants — `CheckSerialization`,
  `GeneratorFinished`, `SerializeFinished`, `SharedCheckSerialization`,
  `SharedGeneratorFinished`, `SharedSerializeFinished`
- **Channel capacity:** Unbounded (no backpressure, TUI never blocks)
- **Message content:** Include artifact ID in every message for dispatch context
- **Directionality:**
  - Foreground → Background: `Effect` messages (what to execute)
  - Background → Foreground: `EffectResult` messages (outcomes)
- **Sequential processing:** Effects execute FIFO in single background task
- **Error Handling:** Errors travel in result messages, not separate error
  channel

### Dependencies

- Uses existing tokio from ratatui (no new deps)
- tokio::sync::mpsc for channels
- tokio::task for background spawn
  </context>

<tasks>

<task type="auto">
  <name>Create channel message types</name>
  <files>src/tui/channels.rs</files>
  <action>
Create src/tui/channels.rs with two enums:

1. EffectCommand (sent foreground → background):
   - CheckSerialization { artifact_index: usize, artifact_name: String, target:
     String, target_type: TargetType }
   - RunGenerator { artifact_index: usize, artifact_name: String, target:
     String, target_type: TargetType, prompts: HashMap<String, String> }
   - Serialize { artifact_index: usize, artifact_name: String, target: String,
     target_type: TargetType, out_dir: PathBuf }
   - SharedCheckSerialization { artifact_index: usize, artifact_name: String,
     backend_name: String, nixos_targets: Vec<String>, home_targets: Vec<String>
     }
   - RunSharedGenerator { artifact_index: usize, artifact_name: String,
     generator_path: String, prompts: HashMap<String, String>, nixos_targets:
     Vec<String>, home_targets: Vec<String>, files: Vec<String> }
   - SharedSerialize { artifact_index: usize, artifact_name: String,
     backend_name: String, out_dir: PathBuf, nixos_targets: Vec<String>,
     home_targets: Vec<String> }

2. EffectResult (sent background → foreground):
   - CheckSerialization { artifact_index: usize, result: Result<bool, String>,
     output: Option<CheckOutput> }
   - GeneratorFinished { artifact_index: usize, result: Result<GeneratorOutput,
     String> }
   - SerializeFinished { artifact_index: usize, result: Result<SerializeOutput,
     String> }
   - SharedCheckSerialization { artifact_index: usize, result: Result<bool,
     String>, output: Option<CheckOutput> }
   - SharedGeneratorFinished { artifact_index: usize, result:
     Result<GeneratorOutput, String> }
   - SharedSerializeFinished { artifact_index: usize, result:
     Result<SerializeOutput, String> }

Add derives: Debug, Clone for both enums. Use same types from app/message.rs for
CheckOutput, GeneratorOutput, SerializeOutput, TargetType. Add required imports.
</action>
<verify>File src/tui/channels.rs compiles without errors</verify>
<done>File exists with all EffectCommand and EffectResult variants, each
containing artifact_index</done>
</task>

<task type="auto">
  <name>Update Cargo.toml for tokio</name>
  <files>Cargo.toml</files>
  <action>
Check if tokio is already in Cargo.toml. If not, add it:

```toml
tokio = { version = "1.42", features = ["sync", "rt", "rt-multi-thread", "macros"] }
```

If ratatui is present, verify tokio is available (ratatui 0.29+ requires tokio
internally). The features needed:

- "sync" for mpsc channels
- "rt" and "rt-multi-thread" for runtime
- "macros" for #[tokio::main]
  </action>
  <verify>Cargo.toml contains tokio dependency with required features</verify>
  <done>tokio dependency present with sync, rt, rt-multi-thread, macros
  features</done>
  </task>

<task type="auto">
  <name>Export channels module</name>
  <files>src/tui/mod.rs</files>
  <action>
Add `pub mod channels;` to src/tui/mod.rs. If mod.rs doesn't exist, create it.

Ensure channels module is accessible from other tui modules.
</action>
<verify>channels module is declared and accessible</verify>
<done>pub mod channels exists in src/tui/mod.rs</done>
</task>

</tasks>

<verification>
- [ ] src/tui/channels.rs compiles successfully
- [ ] EffectCommand enum has all 6 variants with artifact_index field
- [ ] EffectResult enum has all 6 variants with artifact_index field
- [ ] Cargo.toml has tokio with required features
- [ ] Module is exported in src/tui/mod.rs
- [ ] All variants are Clone + Debug
</verification>

<success_criteria>

- Channel types exist for two-way communication
- Each message includes artifact_index for dispatch
- Unbounded mpsc channels will be used (confirmed in design)
- Code compiles without errors </success_criteria>

<output>
After completion, create `.opencode/agents/plans/01-foundation/01-01-SUMMARY.md`
</output>
