---
phase: 06-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pkgs/artifacts/tests/e2e/mod.rs, pkgs/artifacts/src/cli/headless.rs]
autonomous: true

must_haves:
  truths:
    - "Headless API has helper functions for backend storage verification"
    - "E2E test module has utilities for artifact existence checks"
    - "Test utilities support both single-machine and shared artifacts"
  artifacts:
    - path: "pkgs/artifacts/tests/e2e/mod.rs"
      provides: "Enhanced test utilities with backend verification"
      exports: ["verify_artifact_exists", "verify_artifact_content", "load_example"]
    - path: "pkgs/artifacts/src/cli/headless.rs"
      provides: "Headless API with storage introspection"
      exports: ["generate_single_artifact", "generate_shared_artifact"]
  key_links:
    - from: "tests/e2e/mod.rs"
      to: "src/cli/headless.rs"
      via: "use artifacts::cli::headless"
      pattern: "use artifacts::cli::headless"
    - from: "tests/e2e/mod.rs"
      to: "examples/scenarios"
      via: "load_example helper"
      pattern: "load_example\\\(\\\"scenarios/"
---

<objective>
Enhance the end-to-end test infrastructure with helper functions for backend storage verification.

Purpose: Provide reusable test utilities for verifying artifacts are created and stored correctly. Test infrastructure is foundational for meeting all TEST requirements.
Output: Enhanced `tests/e2e/mod.rs` with complete test helpers.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/phases/06-integration-testing/06-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

@pkgs/artifacts/tests/e2e/mod.rs
@pkgs/artifacts/src/cli/headless.rs
@pkgs/artifacts/src/backend/serialization.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend verification helpers to e2e module</name>
  <files>pkgs/artifacts/tests/e2e/mod.rs</files>
  <action>
    Add the following helper functions to tests/e2e/mod.rs after the existing verify_file_content function:

    1. `verify_artifact_exists(storage_dir: &Path, artifact_name: &str) -> Result<()>`
       - Check if the artifact file exists in the backend storage directory
       - Use pattern matching or exact path lookup based on test backend behavior
       - Return meaningful error message if file is missing

    2. `verify_artifact_content(storage_dir: &Path, artifact_name: &str, expected: &str) -> Result<()>`
       - Combine existence check with content verification
       - Read file content and compare with expected
       - Return detailed mismatch error

    3. `get_artifact_path(storage_dir: &Path, artifact_name: &str) -> PathBuf`
       - Return the expected path for an artifact in backend storage
       - Account for test backend's storage pattern (flat or structured)

    Reference existing test patterns in mod.rs. Use `#[cfg(test)]` module or keep in main test module. Follow existing code style with anyhow for error handling.
  </action>
  <verify>cargo test --test tests -- e2e::verify_artifact_exists --test-threads=1 --ignored 2>&1 | grep -q "test result:"</verify>
  <done>Helper functions exist and compile, tests use them successfully</done>
</task>

<task type="auto">
  <name>Task 2: Complete TEST-01 and TEST-02 with infrastructure</name>
  <files>pkgs/artifacts/tests/e2e/mod.rs</files>
  <action>
    Enhance existing `e2e_single_artifact_is_created` test to fully satisfy:
    - TEST-01: Programmatic invocation via headless API (already done)
    - TEST-02: Single artifact creation with simple configuration

    Update the test to:
    1. Verify artifact files exist after generation using new helpers
    2. Check artifact content matches expected format
    3. Include comments documenting which TEST requirements are covered
    4. Ensure test uses `#[serial]` attribute to prevent Nix flake evaluation conflicts

    The test should demonstrate the complete flow:
    - Load example configuration
    - Generate artifact via headless API
    - Verify artifact exists in backend storage
    - Verify artifact content is correct

    Add inline comments like:
    // TEST-01: Programmatic invocation without TUI
    // TEST-02: Creates single artifact with simple configuration
  </action>
  <verify>cargo test --test tests e2e_single_artifact_is_created -- --test-threads=1 2>&1 | grep -E "test result: ok|passed"</verify>
  <done>Test passes, demonstrates programmatic invocation and artifact creation</done>
</task>

<task type="auto">
  <name>Task 3: Add artifact removal/cleanup helper</name>
  <files>pkgs/artifacts/tests/e2e/mod.rs</files>
  <action>
    Add a helper function for test isolation:
    
    `cleanup_test_artifacts(storage_dir: &Path, artifact_names: &[&str]) -> Result<()>`
    - Remove specified artifacts from backend storage
    - Useful for ensuring clean state between tests
    - Return error if cleanup fails

    Also add documentation comments to all helper functions explaining:
    - Purpose and usage
    - Expected parameters
    - Return value semantics
    - When to use (test context)

    Follow the existing code style in mod.rs. Use anyhow::Context for error messages.
  </action>
  <verify>cargo test --test tests -- --test-threads=1 2>&1 | grep -E "test result: ok|running [0-9]+ tests"</verify>
  <done>All e2e tests compile and run, cleanup helper available</done>
</task>

</tasks>

<verification>
- All helper functions compile without errors
- cargo test --test tests runs successfully
- e2e_single_artifact_is_created test passes
- New helpers are documented and usable by other tests
</verification>

<success_criteria>
- tests/e2e/mod.rs has complete set of verification helpers
- Headless API is fully accessible from tests
- TEST-01 and TEST-02 requirements are satisfied
- Tests demonstrate programmatic artifact generation
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-testing/06-01-SUMMARY.md`
</output>
