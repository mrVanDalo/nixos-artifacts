---
phase: 06-integration-testing
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: [pkgs/artifacts/tests/e2e/shared_artifact.rs, pkgs/artifacts/tests/tests.rs]
autonomous: true

must_haves:
  truths:
    - "Shared artifact tests exist (TEST-05)"
    - "Tests run in CI and fail meaningfully when artifacts not created (TEST-06)"
    - "Tests cover both single-machine and shared artifacts"
  artifacts:
    - path: "pkgs/artifacts/tests/e2e/shared_artifact.rs"
      provides: "Shared artifact end-to-end tests"
      exports: ["e2e_shared_artifact_generation", "e2e_shared_artifact_multi_machine"]
    - path: "pkgs/artifacts/tests/tests.rs"
      provides: "Test entry point"
      exports: []
  key_links:
    - from: "tests/e2e/shared_artifact.rs"
      to: "tests/e2e/mod.rs"
      via: "mod import"
      pattern: "mod e2e"
    - from: "tests/e2e/shared_artifact.rs"
      to: "src/cli/headless.rs"
      via: "headless API for shared artifacts"
      pattern: "generate_shared_artifact"
---

<objective>
Create shared artifact tests and CI integration to satisfy TEST-05 and TEST-06.

Purpose: Ensure shared artifacts (across multiple machines) are properly handled, and tests run reliably in CI with meaningful failure messages.
Output: New `tests/e2e/shared_artifact.rs` with shared artifact coverage, CI-ready test configuration.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/phases/06-integration-testing/06-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@pkgs/artifacts/tests/e2e/mod.rs
@pkgs/artifacts/src/cli/headless.rs
@pkgs/artifacts/tests/tests.rs
@examples/scenarios/multiple-machines/flake.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared_artifact.rs test module</name>
  <files>pkgs/artifacts/tests/e2e/shared_artifact.rs</files>
  <action>
    Create `tests/e2e/shared_artifact.rs` for testing shared artifacts across machines.

    Module structure:
    1. Module documentation: "Shared artifact tests for TEST-05"
    2. Imports from super and crates
    3. Helper functions for shared artifact testing
    4. Test functions

    Implement test: `e2e_shared_artifact_generation`
    - Uses multiple-machines scenario
    - Generates a shared artifact for machine-one
    - Verifies artifact exists in backend
    - Verifies artifact is accessible (conceptually) to machine-two
    - Uses #[serial]

    Implement test: `e2e_shared_artifact_multi_machine`
    - Tests that shared artifact can be referenced by multiple machines
    - Generate artifact once, verify accessible to both machine-one and machine-two
    - Tests the shared serialization path ($machines/$users env vars)
    - Verifies shared_serialize script was used

    Implement test: `e2e_shared_artifact_single_instance`
    - Verifies shared artifact is generated only once
    - Generate for machine-one
    - Generate for machine-two (should use existing, not regenerate)
    - Verify single backend artifact file exists

    Requirements:
    - Each test uses #[serial] to prevent conflicts
    - Tests reference TEST-05 in comments
    - Use multiple-machines scenario from examples/scenarios/
    - Handle shared artifact's unique properties (shared=true, different serialization)
  </action>
  <verify>cargo test --test tests e2e_shared_artifact -- --test-threads=1 2>&1 | grep -E "test result: ok|passed"</verify>
  <done>3 shared artifact tests exist and pass</done>
</task>

<task type="auto">
  <name>Task 2: Add CI-ready test configuration</name>
  <files>pkgs/artifacts/tests/tests.rs, pkgs/artifacts/Cargo.toml</files>
  <action>
    Ensure tests are CI-ready with meaningful failure messages (TEST-06):

    1. Update `tests/tests.rs` to ensure proper test discovery:
       Verify all e2e modules are included and run

    2. Check `Cargo.toml` for test configuration:
       - Ensure serial_test dependency is present
       - Verify test dependencies are correct
       - Check for test timeouts or other CI-relevant settings

    3. Document test requirements in comments:
       - Nix must be installed
       - Flake evaluation requires network (for first run)
       - Tests use #[serial] and run single-threaded
       - Expected runtime per test

    4. Verify test failure messages are meaningful:
       - "Artifact not found at expected path: {path}"
       - "Content mismatch: expected {expected}, got {actual}"
       - "Failed to generate artifact: {error}"
       - Should clearly indicate what went wrong

    Run full test suite: cargo test --test tests -- --test-threads=1
    Verify all e2e tests pass and failure messages are clear.
  </action>
  <verify>cargo test --test tests -- --test-threads=1 2>&1 | grep "test result:"</verify>
  <done>All tests pass, meaningful error messages documented</done>
</task>

<task type="auto">
  <name>Task 3: Mark requirements complete and update documentation</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Update project documentation to reflect completed tests:

    1. Update `.planning/REQUIREMENTS.md`:
       Mark all TEST requirements as complete:
       - [x] TEST-01: Test can programmatically invoke artifact generation
       - [x] TEST-02: Test creates single artifact
       - [x] TEST-03: Test verifies artifact exists
       - [x] TEST-04: Test verifies content format
       - [x] TEST-05: Test covers single and shared artifacts
       - [x] TEST-06: Tests run in CI with meaningful failures

    2. Update `.planning/STATE.md`:
       - Update "Current Position" to show Phase 6 complete
       - Update progress bar for v2.0
       - Mark Phase 6 as complete in TODOs
       - Note: Tests are ready for Phase 7 code quality refactoring

    3. Run final verification:
       cargo test --test tests e2e -- --test-threads=1
       Count total e2e tests: should be 6+ tests covering all requirements

    Ensure all changes are accurate and reflect actual test status.
  </action>
  <verify>grep -c "^\s*- \[x\] TEST-" .planning/REQUIREMENTS.md | grep -q "6"</verify>
  <done>All 6 TEST requirements marked complete, STATE.md updated</done>
</task>

</tasks>

<verification>
- cargo test --test tests e2e runs all tests successfully
- Shared artifact tests pass
- All 6 TEST requirements are marked complete
- Tests fail meaningfully when artifact creation breaks
</verification>

<success_criteria>
- shared_artifact.rs exists with comprehensive tests
- TEST-05 requirement satisfied (covers single and shared artifacts)
- TEST-06 requirement satisfied (CI-ready with meaningful failures)
- All 6 TEST requirements marked complete in REQUIREMENTS.md
- STATE.md updated to reflect Phase 6 completion
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-testing/06-03-SUMMARY.md`
</output>
