---
phase: 06-integration-testing
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: [pkgs/artifacts/tests/e2e/edge_cases.rs, pkgs/artifacts/tests/e2e/mod.rs]
autonomous: true

must_haves:
  truths:
    - "Edge case tests exist for error scenarios"
    - "Tests verify proper handling of malformed configurations"
    - "Tests verify failure modes produce meaningful error messages"
  artifacts:
    - path: "pkgs/artifacts/tests/e2e/edge_cases.rs"
      provides: "Edge case and error scenario tests"
      exports: ["e2e_missing_artifact_config", "e2e_invalid_backend", "e2e_generator_failure"]
  key_links:
    - from: "tests/e2e/edge_cases.rs"
      to: "tests/e2e/mod.rs"
      via: "mod import"
      pattern: "mod e2e"
---

<objective>
Create comprehensive edge case tests covering error scenarios, malformed configurations, and failure modes.

Purpose: Ensure the system gracefully handles edge cases and produces meaningful
error messages. Tests should verify that failures are caught early and reported
clearly. Output: New `tests/e2e/edge_cases.rs` with error scenario tests.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/phases/06-integration-testing/06-RESEARCH.md
@.planning/phases/06-integration-testing/06-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@pkgs/artifacts/tests/e2e/mod.rs @pkgs/artifacts/src/cli/headless.rs
@examples/scenarios/error-missing-files/flake.nix
@examples/scenarios/error-missing-generator/flake.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create edge_cases.rs test module</name>
  <files>pkgs/artifacts/tests/e2e/edge_cases.rs</files>
  <action>
    Create `tests/e2e/edge_cases.rs` for testing error scenarios and edge cases.

    Module structure:
    1. Module documentation explaining purpose (edge cases and error handling)
    2. Imports from super and crates
    3. Helper functions for error scenario testing
    4. Test functions covering edge cases

    Implement test: `e2e_missing_artifact_config`
    - Attempts to generate artifact that doesn't exist in config
    - Expects graceful error with clear message
    - Verifies error is returned, not panicked
    - Uses #[serial]

    Implement test: `e2e_invalid_backend`
    - Uses config with non-existent backend
    - Verifies error message mentions missing backend
    - Tests backend validation happens before generation

    Implement test: `e2e_generator_failure`
    - Uses scenario where generator exits with error
    - Verifies proper error propagation
    - Checks that partial artifacts are not left behind

    Implement test: `e2e_serialization_failure`
    - Simulates backend serialization error
    - Verifies artifact generation is still successful
    - Checks error message mentions serialization specifically

    Implement test: `e2e_empty_artifact_name`
    - Tests artifact with empty or whitespace-only name
    - Verifies validation catches this early

    Implement test: `e2e_special_characters_in_artifact_name`
    - Tests artifact names with special characters (spaces, hyphens, underscores)
    - Verifies proper handling and escaping

    Requirements:
    - Each test uses #[serial] to prevent conflicts
    - Tests use existing error scenarios in examples/scenarios/
    - Error messages are validated for clarity and actionability
    - Tests verify error types (not just strings)

</action>
  <verify>cargo test --test tests e2e_edge -- --test-threads=1 2>&1 | grep -E "test result: ok|passed"</verify>
  <done>6+ edge case tests exist and pass</done>
</task>

<task type="auto">
  <name>Task 2: Add error message validation tests</name>
  <files>pkgs/artifacts/tests/e2e/edge_cases.rs</files>
  <action>
    Add tests that specifically validate error message quality:

    Test: `e2e_error_message_contains_context`
    - When generation fails, error includes:
      - Artifact name
      - Target machine (if applicable)
      - Generator path
      - Exit code or error type

    Test: `e2e_error_message_actionable`
    - Error messages provide next steps or hints
    - "Check generator script at: {path}"
    - "Verify backend configuration: {backend}"
    - "See logs for details: {log_path}"

    Test: `e2e_error_message_not_internal`
    - Error messages don't expose internal Rust details
    - No "unwrap failed" or "thread panicked" messages
    - User-facing messages only

    Test: `e2e_multiple_failures_reported`
    - When multiple artifacts fail, all errors are collected
    - Not just first error reported
    - Each failure has its own context

    Requirements:
    - Use regex or string matching to validate message content
    - Tests should be resilient to minor wording changes
    - Focus on presence of key information, not exact wording

</action>
  <verify>cargo test --test tests e2e_error -- --test-threads=1 2>&1 | grep -E "test result: ok|running [0-9]+ tests"</verify>
  <done>4+ error message validation tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Integrate edge_cases module and verify</name>
  <files>pkgs/artifacts/tests/e2e/mod.rs, pkgs/artifacts/tests/tests.rs</files>
  <action>
    Ensure the new edge_cases module is properly integrated:

    1. Add to `tests/e2e/mod.rs`:
       ```rust
       pub mod edge_cases;
       ```

    2. Verify `tests/tests.rs` includes the e2e module:
       Should already have `mod e2e;` or similar

    3. Run complete test suite to verify integration:
       cargo test --test tests e2e -- --test-threads=1

    4. Document error scenarios in module comments:
       - List of error scenarios covered
      - How to add new error scenarios
       - Guidelines for error message quality

    Fix any compilation issues:
    - Missing imports
    - Visibility issues
    - Path resolution problems

</action>
  <verify>cargo test --test tests e2e -- --test-threads=1 2>&1 | tail -10 | grep -q "test result:"</verify>
  <done>All edge case tests compile and run, module properly integrated</done>
</task>

</tasks>

<verification>
- cargo test --test tests e2e runs all tests including edge cases
- All edge case tests pass
- Error messages are validated for quality
- Module is properly integrated into test suite
</verification>

<success_criteria>

- edge_cases.rs exists with comprehensive error scenario tests
- Tests cover: missing config, invalid backend, generator failure, serialization
  failure
- Error messages are validated for context, actionability, and user-friendliness
- All tests pass with #[serial] isolation </success_criteria>

<output>
After completion, create `.planning/phases/06-integration-testing/06-04-SUMMARY.md`
</output>
