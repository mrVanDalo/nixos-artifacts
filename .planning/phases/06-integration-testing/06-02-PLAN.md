---
phase: 06-integration-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [pkgs/artifacts/tests/e2e/backend_verify.rs]
autonomous: true

must_haves:
  truths:
    - "Backend verification tests exist for artifact storage"
    - "Tests verify artifacts exist at expected backend location (TEST-03)"
    - "Tests verify artifact content matches expected format (TEST-04)"
  artifacts:
    - path: "pkgs/artifacts/tests/e2e/backend_verify.rs"
      provides: "Backend storage verification tests"
      exports: ["e2e_backend_storage_single_artifact", "e2e_backend_storage_shared_artifact"]
  key_links:
    - from: "tests/e2e/backend_verify.rs"
      to: "tests/e2e/mod.rs"
      via: "mod import and helper functions"
      pattern: "use super::\*"
    - from: "tests/e2e/backend_verify.rs"
      to: "src/backend/serialization.rs"
      via: "serialization verification"
      pattern: "ARTIFACTS_TEST_OUTPUT_DIR"
---

<objective>
Create comprehensive backend verification tests that verify artifacts are actually stored in the backend with correct content.

Purpose: Satisfy TEST-03 (verify artifact exists at backend location) and TEST-04 (verify content format). These tests ensure artifacts aren't just generated but properly persisted.
Output: New `tests/e2e/backend_verify.rs` file with backend storage tests.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/phases/06-integration-testing/06-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

@pkgs/artifacts/tests/e2e/mod.rs
@pkgs/artifacts/src/backend/serialization.rs
@pkgs/artifacts/src/cli/headless.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend_verify.rs test module</name>
  <files>pkgs/artifacts/tests/e2e/backend_verify.rs</files>
  <action>
    Create a new test file `tests/e2e/backend_verify.rs` that focuses specifically on backend storage verification.

    Structure:
    1. Module documentation explaining purpose (TEST-03, TEST-04)
    2. Imports from super (mod.rs helpers) and crates
    3. Test functions for verifying backend storage

    Implement test: `e2e_backend_storage_single_artifact`
    - Uses single-artifact-with-prompts scenario
    - Generates artifact via headless API
    - Verifies artifact exists in backend storage location
    - Verifies content matches expected format
    - Uses #[serial] to prevent conflicts

    Implement test: `e2e_backend_storage_content_format`
    - Generates artifact with known content
    - Verifies file content matches exactly what generator produced
    - Tests that serialization preserves content integrity

    Key requirements:
    - Use tempfile for storage directory
    - Set ARTIFACTS_TEST_OUTPUT_DIR environment variable for test backend
    - Use helpers from mod.rs for verification
    - Include inline comments: // TEST-03, // TEST-04
  </action>
  <verify>cargo test --test tests e2e_backend_storage -- --test-threads=1 2>&1 | grep -E "test result: ok|passed"</verify>
  <done>backend_verify.rs compiles, both tests pass, verify artifact storage and content</done>
</task>

<task type="auto">
  <name>Task 2: Add content verification edge cases</name>
  <files>pkgs/artifacts/tests/e2e/backend_verify.rs</files>
  <action>
    Add additional test cases to backend_verify.rs:

    Test: `e2e_backend_storage_binary_content`
    - Tests that binary content is preserved through serialization
    - Generate artifact with non-text content (if scenario supports it)
    - Or use a scenario with base64 encoded content

    Test: `e2e_backend_storage_multiple_files`
    - Tests artifact with multiple files
    - Verifies ALL files exist in backend storage
    - Verifies EACH file has correct content

    Test: `e2e_backend_storage_persists`
    - Verifies artifact persists after generation completes
    - Checks file exists after temp cleanup (for actual storage)
    - For test backend, verifies in specified output directory

    Requirements:
    - Each test uses #[serial] attribute
    - Tests use descriptive names indicating what's being verified
    - Comments reference TEST-03 and TEST-04
    - All tests follow pattern: setup -> generate -> verify -> cleanup
  </action>
  <verify>cargo test --test tests e2e_backend_storage -- --test-threads=1 2>&1 | grep -E "test result: ok.*[3-9]|test result: ok.*1[0-9]"</verify>
  <done>4+ backend storage tests exist and pass</done>
</task>

<task type="auto">
  <name>Task 3: Update tests.rs to include backend_verify module</name>
  <files>pkgs/artifacts/tests/tests.rs, pkgs/artifacts/tests/e2e/mod.rs</files>
  <action>
    Ensure the new backend_verify module is properly integrated:

    1. Add to `tests/e2e/mod.rs`:
       ```rust
       pub mod backend_verify;
       ```
       OR use pub mod if items need to be public

    2. Verify `tests/tests.rs` includes the e2e module:
       Should already have `mod e2e;` or similar

    3. Run complete test suite to verify integration:
       cargo test --test tests

    Fix any compilation issues:
    - Missing imports
    - Visibility issues
    - Path resolution problems

    Document the test structure in module comments explaining:
    - What each test verifies
    - How to run specific test categories
    - Prerequisites (Nix installation, flake evaluation)
  </action>
  <verify>cargo test --test tests -- --test-threads=1 2>&1 | tail -5 | grep -q "test result:"</verify>
  <done>All tests compile and run, backend_verify module integrated</done>
</task>

</tasks>

<verification>
- cargo test --test tests e2e_backend_storage runs successfully
- All backend verification tests pass
- TEST-03 requirement is satisfied (artifact exists at backend location)
- TEST-04 requirement is satisfied (content matches expected format)
</verification>

<success_criteria>
- backend_verify.rs exists with comprehensive tests
- Tests verify artifacts exist in backend storage
- Tests verify artifact content integrity
- TEST-03 and TEST-04 marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-testing/06-02-SUMMARY.md`
</output>
