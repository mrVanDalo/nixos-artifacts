---
phase: 17-model-based-testing-with-full-state-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/tests/tui/model_state.rs
  - pkgs/artifacts/tests/tui/mod.rs
autonomous: true

must_haves:
  truths:
    - "ModelState struct exists and captures full Model state via Debug trait"
    - "ModelState can be created from any Model instance"
    - "ModelState is shared between integration tests and view tests"
    - "Tests can capture both Model state AND rendered view in single assertion"
  artifacts:
    - path: "pkgs/artifacts/tests/tui/model_state.rs"
      provides: "Shared ModelState struct for test state capture"
      min_lines: 80
      exports: ["ModelState", "ArtifactState"]
    - path: "pkgs/artifacts/tests/tui/mod.rs"
      provides: "Test module exports"
      contains: "pub mod model_state"
  key_links:
    - from: "tests/tui/model_state.rs"
      to: "src/app/model.rs"
      via: "imports Model, Screen, ListEntry, ArtifactStatus"
      pattern: "use artifacts::app::model::"
    - from: "tests/tui/integration_tests.rs"
      to: "tests/tui/model_state.rs"
      via: "uses shared ModelState"
      pattern: "use crate::tui::model_state::"
    - from: "tests/tui/view_tests.rs"
      to: "tests/tui/model_state.rs"
      via: "uses shared ModelState"
      pattern: "use crate::tui::model_state::"
---

<objective>
Create shared test infrastructure for capturing full Model state using the Debug trait pattern from integration tests. Extract the ModelState struct from integration_tests.rs into a reusable module that both integration tests and view tests can share.

Purpose: Enable view tests to capture full Model state alongside rendered
output, documenting the Elm Architecture pattern of inputs → Model
transformations → View rendering. Output: Shared model_state.rs module + updated
mod.rs exports.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-backend-dev-docs/16-03-SUMMARY.md
@pkgs/artifacts/tests/tui/integration_tests.rs
@pkgs/artifacts/tests/tui/view_tests.rs
@pkgs/artifacts/tests/tui/mod.rs
@pkgs/artifacts/src/app/model.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared model_state.rs module</name>
  <files>pkgs/artifacts/tests/tui/model_state.rs</files>
  <action>
Create `pkgs/artifacts/tests/tui/model_state.rs` containing:

1. Import necessary types from `artifacts::app::model`:
   - `Model`, `Screen`, `ListEntry`, `ArtifactStatus`, `TargetType`

2. Define `ModelState` struct with `#[derive(Debug)]`:
   ```rust
   #[derive(Debug)]
   pub struct ModelState {
       pub screen: &'static str,
       pub selected_index: usize,
       pub selected_log_step: &'static str,
       pub artifacts: Vec<ArtifactState>,
       pub error: Option<String>,
       pub warnings_count: usize,
   }
   ```

3. Define `ArtifactState` struct with `#[derive(Debug)]`:
   ```rust
   #[derive(Debug)]
   pub struct ArtifactState {
       pub target: String,
       pub name: String,
       pub status: String,
       pub target_type: &'static str,
   }
   ```

4. Implement `ModelState::from_model(model: &Model) -> Self`:
   - Map `Screen` enum to string labels (ArtifactList, SelectGenerator, etc.)
   - Convert `model.entries` to `Vec<ArtifactState>`:
     - For Single entries: use target, artifact.name, status, target_type
     - For Shared entries: use "[shared]" as target, info.artifact_name as name
   - Normalize status strings (use `format!("{:?}", status)`)
   - Include `selected_log_step.label()`
   - Count warnings

5. Add helper function `normalize_status(status: String) -> String` to strip
   project-specific paths if needed (similar to integration_tests.rs but
   simplified).

Per user decision: Keep existing file structure, use derive(Debug) for automatic
field capture, follow the pattern from integration_tests.rs.
</action>
<verify>cargo check --tests passes without errors</verify>
<done>model_state.rs exists with ModelState and ArtifactState structs, both
deriving Debug, with from_model() method implemented</done>
</task>

<task type="auto">
  <name>Task 2: Export model_state from tests/tui/mod.rs</name>
  <files>pkgs/artifacts/tests/tui/mod.rs</files>
  <action>
Update `pkgs/artifacts/tests/tui/mod.rs` to export the new model_state module:

1. Add `pub mod model_state;` to the module declarations
2. Ensure the module structure allows other test files to import:
   - `use crate::tui::model_state::{ModelState, ArtifactState};`

If mod.rs doesn't exist yet (check first), create it with:

```rust
pub mod model_state;
pub mod view_tests;
pub mod integration_tests;
```

Per user decision: Keep existing file structure - only modify mod.rs to add the
new module export.
</action>
<verify>cargo check --tests passes, module is accessible from other test
files</verify>
<done>mod.rs exports model_state module, other test files can import
ModelState</done>
</task>

<task type="auto">
  <name>Task 3: Refactor integration_tests.rs to use shared ModelState</name>
  <files>pkgs/artifacts/tests/tui/integration_tests.rs</files>
  <action>
Refactor `pkgs/artifacts/tests/tui/integration_tests.rs` to use the shared ModelState:

1. Remove the local `ModelState` struct definition (lines ~200-250)
2. Remove the local `ArtifactState` struct definition
3. Remove the `impl ModelState` block with `from_model`
4. Remove the `normalize_status` function (now in model_state.rs)
5. Add import: `use crate::tui::model_state::{ModelState, ArtifactState};`
6. Update `TestResult` struct to use the imported `ModelState`
7. Ensure all existing tests still compile and run

The integration tests should now serve as a proof-of-concept that the shared
ModelState works correctly.

Per user decision: Same file structure, just reorganize by using the shared
module instead of local definitions.
</action>
<verify>cargo test --test tests test_main runs successfully (integration tests
still pass)</verify>
<done>integration_tests.rs uses shared ModelState from model_state module, all
existing tests pass</done>
</task>

</tasks>

<verification>
1. Run `cargo check --tests` - no compilation errors
2. Run `cargo test --test tests scenario_simple_generate_one` - integration test passes with shared ModelState
3. Verify `ModelState::from_model()` is accessible from both integration_tests.rs and view_tests.rs
4. Check that Debug output includes all relevant fields (screen, selected_index, artifacts, etc.)
</verification>

<success_criteria>

- Shared `tests/tui/model_state.rs` module exists and compiles
- `ModelState` and `ArtifactState` structs derive Debug for automatic field
  capture
- Integration tests refactored to use shared module (tests still pass)
- Both integration tests and view tests can import and use
  `ModelState::from_model()`
- File structure preserved per user decision (modules within same files, clear
  separation) </success_criteria>

<output>
After completion, create `.planning/phases/17-model-based-testing-with-full-state-capture/17-01-SUMMARY.md`
</output>
