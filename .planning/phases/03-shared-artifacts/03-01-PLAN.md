---
phase: 03-shared-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/src/tui/background.rs
autonomous: true

must_haves:
  truths:
    - "SharedCheckSerialization executes actual check script in background"
    - "RunSharedGenerator runs actual generator in bubblewrap container"
    - "SharedSerialize executes actual shared_serialize script for all targets"
    - "All three effects use spawn_blocking for non-blocking execution"
    - "Error handling captures and returns script failures"
  artifacts:
    - path: "pkgs/artifacts/src/tui/background.rs"
      provides: "Real shared effect implementations replacing TODO stubs"
      contains: "spawn_blocking for SharedCheckSerialization, RunSharedGenerator, SharedSerialize"
  key_links:
    - from: "background.rs SharedCheckSerialization handler"
      to: "serialization.rs run_shared_check_serialization()"
      via: "spawn_blocking wrapper"
    - from: "background.rs RunSharedGenerator handler"
      to: "generator.rs run_generator_script_with_path()"
      via: "spawn_blocking wrapper"
    - from: "background.rs SharedSerialize handler"
      to: "serialization.rs run_shared_serialize()"
      via: "spawn_blocking wrapper"
---

<objective>
Replace TODO stubs in the background task with real implementations for shared artifact effects.

Purpose: Complete Phase 3 by implementing SharedCheckSerialization,
RunSharedGenerator, and SharedSerialize effects using the existing backend
functions. Output: Fully functional shared artifact generation in the TUI
background task.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@pkgs/artifacts/src/tui/background.rs
@pkgs/artifacts/src/backend/serialization.rs
@pkgs/artifacts/src/backend/generator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SharedCheckSerialization Effect</name>
  <files>pkgs/artifacts/src/tui/background.rs</files>
  <action>
    Replace the TODO stub for EffectCommand::SharedCheckSerialization (around line 378-392) with a real implementation that:

    1. Extracts artifact_index, artifact_name, targets, and target_types from the command
    2. Uses tokio::task::spawn_blocking to call run_shared_check_serialization() from crate::backend::serialization
    3. The function signature is:
       ```rust
       pub fn run_shared_check_serialization(
           artifact_name: &str,
           backend_name: &str,
           backend: &BackendConfiguration,
           make: &MakeConfiguration,
           nixos_targets: &[String],
           home_targets: &[String],
       ) -> Result<CheckResult>
       ```
    4. Split targets into nixos_targets and home_targets based on target_types
    5. Get backend_name from make.get_shared_artifacts() using artifact_name
    6. Handle the Result:
       - On success: CheckResult.needs_generation becomes the single boolean result for all targets (shared artifacts are all-or-nothing)
       - On error: Return all targets needing generation with the error message
    7. Return EffectResult::SharedCheckSerialization with:
       - artifact_index
       - needs_generation: Vec&lt;bool&gt; (one per target)
       - outputs: Vec&lt;Option&lt;String&gt;&gt; (captured output or error per target)

    Pattern to follow: Look at CheckSerialization implementation (lines 250-302) for spawn_blocking pattern.

    Key imports needed: use crate::backend::serialization::run_shared_check_serialization;

</action>
  <verify>
    cargo check passes without errors
    grep -A 30 "EffectCommand::SharedCheckSerialization" pkgs/artifacts/src/tui/background.rs | grep -v "TODO"
  </verify>
  <done>
    SharedCheckSerialization uses spawn_blocking to call run_shared_check_serialization(), returning real check results instead of hardcoded vec![true]. Error handling captures script failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement RunSharedGenerator Effect</name>
  <files>pkgs/artifacts/src/tui/background.rs</files>
  <action>
    Replace the TODO stub for EffectCommand::RunSharedGenerator (around line 394-408) with a real implementation that:

    1. Extracts artifact_index, artifact_name, machine_targets, user_targets, and prompts from the command
    2. Use tokio::task::spawn_blocking to call run_generator_script_with_path() from crate::backend::generator
    3. The function signature is:
       ```rust
       pub fn run_generator_script_with_path(
           generator_path: &str,
           make_base: &Path,
           prompts: &Path,
           out: &Path,
       ) -> Result<CapturedOutput>
       ```
    4. Get generator_path from make.get_shared_artifacts() using artifact_name
    5. Get make_base from make.make_base
    6. Use prompts path from command
    7. Create a temporary output directory (TempFile::new_dir) for $out
    8. Handle the Result:
       - On success: Verify generated files using verify_generated_files() from crate::backend::generator
       - If verification passes: return success with output
       - If verification fails: return failure with error
       - On error: return failure with error message
    9. Return EffectResult::SharedGeneratorFinished with:
       - artifact_index
       - success: bool
       - output: Option&lt;String&gt; (stdout/stderr)
       - error: Option&lt;String&gt; (error message if failed)

    Pattern to follow: Look at RunGenerator implementation (lines 304-349) - very similar but use run_generator_script_with_path instead of run_generator_script.

    Key imports needed: 
    - use crate::backend::generator::{run_generator_script_with_path, verify_generated_files};
    - use crate::backend::tempfile::TempFile;

</action>
  <verify>
    cargo check passes without errors
    grep -A 40 "EffectCommand::RunSharedGenerator" pkgs/artifacts/src/tui/background.rs | grep -v "TODO"
  </verify>
  <done>
    RunSharedGenerator uses spawn_blocking to call run_generator_script_with_path(), creates temp out directory, verifies generated files, and returns real result instead of hardcoded success.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement SharedSerialize Effect</name>
  <files>pkgs/artifacts/src/tui/background.rs</files>
  <action>
    Replace the TODO stub for EffectCommand::SharedSerialize (around line 410-428) with a real implementation that:

    1. Extracts artifact_index, artifact_name, machine_targets, user_targets from the command
    2. Get out_path from self.out_paths (created during generator phase, like single artifact flow)
    3. Use tokio::task::spawn_blocking to call run_shared_serialize() from crate::backend::serialization
    4. The function signature is:
       ```rust
       pub fn run_shared_serialize(
           artifact_name: &str,
           backend_name: &str,
           backend: &BackendConfiguration,
           out: &Path,
           make: &MakeConfiguration,
           nixos_targets: &[String],
           home_targets: &[String],
       ) -> Result<CapturedOutput>
       ```
    5. Get backend_name from make.get_shared_artifacts() using artifact_name
    6. Handle the Result:
       - On success: All targets succeed with captured output
       - On error: All targets fail with error message (shared serialization is atomic)
    7. Return EffectResult::SharedSerializeFinished with:
       - artifact_index
       - results: Vec&lt;(String, bool, Option&lt;String&gt;)&gt; - tuple of (target_name, success, output/error)
    8. Include both machine and user targets in results

    Pattern to follow: Look at Serialize implementation (lines 351-376).

    Key imports needed: use crate::backend::serialization::run_shared_serialize;

    NOTE: This requires out_paths to be populated from generator phase - verify the handler stores it properly (like single artifact handler does with out_path).

</action>
  <verify>
    cargo check passes without errors
    cargo test --lib 2>&1 | tail -5
    grep -A 35 "EffectCommand::SharedSerialize" pkgs/artifacts/src/tui/background.rs | grep -v "TODO"
  </verify>
  <done>
    SharedSerialize uses spawn_blocking to call run_shared_serialize(), passing the out directory from generator phase, and returns real per-target results instead of hardcoded success.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. cargo check passes with no errors
2. cargo test --lib passes (all existing tests still work)
3. The three TODO comments are removed from background.rs
4. All three shared effects use spawn_blocking pattern
5. Error handling matches single artifact pattern
</verification>

<success_criteria>

- SharedCheckSerialization calls run_shared_check_serialization() via
  spawn_blocking
- RunSharedGenerator calls run_generator_script_with_path() via spawn_blocking
  with file verification
- SharedSerialize calls run_shared_serialize() via spawn_blocking with proper
  out path
- All effects return proper EffectResult variants with real data
- No TODO stubs remain for shared effects in background.rs
- Existing tests pass </success_criteria>

<output>
After completion, create `.planning/phases/03-shared-artifacts/03-01-SUMMARY.md`
</output>
