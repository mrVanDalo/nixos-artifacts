---
phase: 03-shared-artifacts
plan: 03
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - pkgs/artifacts/src/tui/runtime.rs
  - pkgs/artifacts/src/tui/background.rs
  - pkgs/artifacts/src/cli/mod.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Debug logging confirms background task spawns and executes"
    - "Logs show commands being sent and results being received"
    - "Artifacts transition from pending to needs-generation/up-to-date"
  artifacts:
    - path: "pkgs/artifacts/src/tui/background.rs"
      provides: "File-based debug logging in background task"
      min_lines: 10
    - path: "pkgs/artifacts/src/tui/runtime.rs"
      provides: "Debug logging in main loop"
      min_lines: 5
  key_links:
    - from: "runtime.rs"
      to: "background.rs"
      via: "spawn_background_task"
      pattern: "spawn_background_task"
---

<objective>
Add comprehensive debug logging to verify the background task execution flow.

Purpose: We need visibility into whether the background task spawns, receives
commands, executes them, and returns results. Console logging is lost when TUI
starts, so we need file-based logging.

Output: Working file-based logging that confirms each step of the background
execution.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-shared-artifacts/03-UAT.md
@.planning/phases/03-shared-artifacts/03-02-SUMMARY.md
@pkgs/artifacts/src/tui/runtime.rs
@pkgs/artifacts/src/tui/background.rs
@pkgs/artifacts/src/cli/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file-based logging module</name>
  <files>pkgs/artifacts/src/logging.rs (new)</files>
  <action>
    Create a new logging module that writes to a file instead of stdout/stderr:

    Create pkgs/artifacts/src/logging.rs with:
    - A static file handle for /tmp/artifacts_debug.log
    - A log_to_file() function that appends timestamped messages
    - Thread-safe using std::sync::Mutex

    Example:
    ```rust
    use std::fs::OpenOptions;
    use std::io::Write;
    use std::sync::Mutex;

    lazy_static::lazy_static! {
        static ref LOG_FILE: Mutex<std::fs::File> = Mutex::new(
            OpenOptions::new()
                .create(true)
                .append(true)
                .open("/tmp/artifacts_debug.log")
                .unwrap()
        );
    }

    pub fn log(msg: &str) {
        let mut file = LOG_FILE.lock().unwrap();
        writeln!(file, "[{}] {}", chrono::Local::now().format("%H:%M:%S%.3f"), msg).ok();
    }
    ```

</action>
  <verify>File compiles and module is accessible</verify>
  <done>Logging module created with file-based output</done>
</task>

<task type="auto">
  <name>Task 2: Add spawn logging to runtime</name>
  <files>pkgs/artifacts/src/tui/runtime.rs</files>
  <action>
    Add logging at key points in run_async():

    1. Before spawn_background_task: "[RUNTIME] Spawning background task"
    2. After spawn: "[RUNTIME] Background task spawned"
    3. In main loop when sending commands: "[RUNTIME] Sending command: {:?}"
    4. In main loop when receiving results: "[RUNTIME] Received result: {:?}"

    Use crate::logging::log() for all log calls.

</action>
  <verify>runtime.rs compiles with new logging calls</verify>
  <done>Runtime logging added at spawn, send, and receive points</done>
</task>

<task type="auto">
  <name>Task 3: Add execution logging to background</name>
  <files>pkgs/artifacts/src/tui/background.rs</files>
  <action>
    Add logging in spawn_background_task():

    1. At task start: "[BACKGROUND] Task started"
    2. When receiving command: "[BACKGROUND] Received command: {:?}"
    3. Before execute: "[BACKGROUND] Executing command"
    4. After execute: "[BACKGROUND] Command complete, result: {:?}"
    5. When sending result: "[BACKGROUND] Sending result"
    6. On channel close: "[BACKGROUND] Channel closed, exiting"

    Use crate::logging::log() for all log calls.

</action>
  <verify>background.rs compiles with new logging calls</verify>
  <done>Background task logging covers full execution lifecycle</done>
</task>

<task type="auto">
  <name>Task 4: Add chrono dependency for timestamps</name>
  <files>pkgs/artifacts/Cargo.toml</files>
  <action>
    Add chrono as a dependency for timestamp formatting:

    chrono = "0.4"

    Also add lazy_static if not already present:
    lazy_static = "1"

    Then regenerate Cargo.lock.

</action>
  <verify>Cargo.toml has chrono and lazy_static dependencies</verify>
  <done>Dependencies added for logging</done>
</task>

<task type="auto">
  <name>Task 5: Export logging module and test</name>
  <files>pkgs/artifacts/src/lib.rs</files>
  <action>
    1. Add pub mod logging; to src/lib.rs
    2. Build with nix build .#artifacts --no-link
    3. Run: rm -f /tmp/artifacts_debug.log && timeout 5 nix run .#artifacts --
    4. Check: cat /tmp/artifacts_debug.log
    5. Verify log shows: RUNTIME spawn, BACKGROUND start, command received/executed
  </action>
  <verify>Log file shows complete execution flow</verify>
  <done>Logging works and shows background task execution</done>
</task>

</tasks>

<verification>
1. rm -f /tmp/artifacts_debug.log
2. timeout 10 nix run .#artifacts --
3. cat /tmp/artifacts_debug.log
4. Verify log contains:
   - [RUNTIME] Spawning background task
   - [BACKGROUND] Task started
   - [RUNTIME] Sending command: CheckSerialization
   - [BACKGROUND] Received command
   - [BACKGROUND] Command complete
   - [RUNTIME] Received result
</verification>

<success_criteria>

- Log file /tmp/artifacts_debug.log is created
- Log shows background task spawning
- Log shows commands being sent and received
- Log shows results being returned
- Artifacts transition from pending to needs-generation (visible in TUI)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-shared-artifacts/03-03-SUMMARY.md`
</output>
