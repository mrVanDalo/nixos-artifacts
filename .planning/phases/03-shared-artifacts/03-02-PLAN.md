---
phase: 03-shared-artifacts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/src/bin/artifacts.rs
  - pkgs/artifacts/Cargo.toml
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Background task spawns and executes concurrently with TUI"
    - "CheckSerialization effects run in background and return results"
    - "TUI remains responsive while background effects execute"
  artifacts:
    - path: "pkgs/artifacts/src/bin/artifacts.rs"
      provides: "Multi-threaded tokio runtime"
      min_lines: 10
    - path: "pkgs/artifacts/Cargo.toml"
      provides: "rt-multi-thread feature enabled"
      contains: 'features = ["sync", "rt", "rt-multi-thread", "macros", "time"]'
  key_links:
    - from: "src/bin/artifacts.rs"
      to: "tokio runtime"
      via: "#[tokio::main]"
      pattern: "tokio::spawn"
---

<objective>
Fix the tokio runtime to properly support concurrent background task execution.

Purpose: The TUI is freezing because the background task never executes - the
single-threaded runtime can't spawn concurrent tasks.

Output: Working multi-threaded runtime where background effects execute while
TUI remains responsive.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-shared-artifacts/03-UAT.md
@.planning/phases/03-shared-artifacts/03-01-SUMMARY.md
@pkgs/artifacts/src/bin/artifacts.rs
@pkgs/artifacts/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Cargo.toml tokio features</name>
  <files>pkgs/artifacts/Cargo.toml</files>
  <action>
    Ensure tokio dependency includes rt-multi-thread feature:

    tokio = { version = "1", features = ["sync", "rt", "rt-multi-thread", "macros", "time"] }

    If missing, add it. Then regenerate Cargo.lock to ensure the feature is picked up by Nix.

</action>
  <verify>grep -q 'rt-multi-thread' pkgs/artifacts/Cargo.toml && cargo check passes</verify>
  <done>Cargo.toml has rt-multi-thread feature enabled</done>
</task>

<task type="auto">
  <name>Task 2: Fix tokio::main to use multi-threaded runtime</name>
  <files>pkgs/artifacts/src/bin/artifacts.rs</files>
  <action>
    Change from:
    #[tokio::main(flavor = "current_thread")]

    To:
    #[tokio::main]

    This uses the default multi-threaded runtime which supports concurrent task spawning.

    Also verify no other files use current_thread runtime.

</action>
  <verify>grep -r 'current_thread' pkgs/artifacts/src/ || echo "No current_thread found"</verify>
  <done>artifacts.rs uses #[tokio::main] without current_thread flavor</done>
</task>

<task type="auto">
  <name>Task 3: Verify clean Nix build with new runtime</name>
  <files>pkgs/artifacts/Cargo.lock</files>
  <action>
    Force a clean Nix build to ensure the new tokio features are used:

    1. Remove Cargo.lock to force fresh dependency resolution
    2. Run cargo generate-lockfile to create new lock
    3. Build with nix build .#artifacts --no-link

    Check that the build succeeds without errors about missing features.

</action>
  <verify>nix build .#artifacts --no-link succeeds</verify>
  <done>Clean Nix build succeeds with multi-threaded runtime</done>
</task>

</tasks>

<verification>
1. Run: nix run .#artifacts -- --log-level debug
2. Check that TUI launches without freezing
3. Press Enter on an artifact
4. Verify TUI remains responsive (can navigate with j/k)
5. Check that artifact status changes from ○ to ◐ after ~1 second
</verification>

<success_criteria>

- Cargo.toml has rt-multi-thread feature
- #[tokio::main] doesn't use current_thread
- Nix build succeeds
- Background task executes (artifacts change from pending to needs-generation)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-shared-artifacts/03-02-SUMMARY.md`
</output>
