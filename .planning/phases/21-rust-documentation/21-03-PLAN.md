---
phase: 21-rust-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/src/config/mod.rs
  - pkgs/artifacts/src/config/backend.rs
  - pkgs/artifacts/src/config/make.rs
  - pkgs/artifacts/src/config/nix.rs
autonomous: true

must_haves:
  truths:
    - "All config modules have module-level documentation"
    - "All public types in config have doc comments"
    - "All public functions in config have doc comments"
  artifacts:
    - path: "pkgs/artifacts/src/config/mod.rs"
      provides: "Module-level documentation for config module"
      min_lines: 15
    - path: "pkgs/artifacts/src/config/backend.rs"
      provides: "Documentation for BackendConfiguration and BackendEntry"
      exports: ["BackendConfiguration", "BackendEntry", "BackendCapabilities"]
    - path: "pkgs/artifacts/src/config/make.rs"
      provides: "Documentation for MakeConfiguration and ArtifactDef"
      exports: ["MakeConfiguration", "ArtifactDef", "FileDef", "PromptDef"]
  key_links:
    - from: "config module docs"
      to: "config/backend"
      via: "pub mod backend"
    - from: "config module docs"
      to: "config/make"
      via: "pub mod make"
---

<objective>
Add comprehensive documentation to the config module and all its submodules.

Purpose: The config module handles parsing of backend.toml and flake.nix configuration. These are the core data structures that define how artifacts are configured.
Output: Documented config module with all public types and functions explained
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.planning/phases/21-rust-documentation/21-01-SUMMARY.md
</execution_context>

<context>
The config module contains:
- backend.rs: BackendConfiguration, BackendEntry, BackendCapabilities parsing from TOML
- make.rs: MakeConfiguration, ArtifactDef, FileDef, PromptDef from Nix flake evaluation
- nix.rs: Nix expression building and evaluation helpers
- mod.rs: Module exports

Current state: Some types already have partial documentation (e.g., BackendCapabilities fields). Need to add module-level docs and complete function/type documentation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document config/mod.rs module</name>
  <files>pkgs/artifacts/src/config/mod.rs</files>
  <action>
    Add comprehensive module-level documentation to src/config/mod.rs.

    The documentation should explain:
    - What the config module does (configuration parsing from TOML and Nix)
    - Key submodules: backend (backend.toml), make (flake.nix), nix (Nix evaluation)
    - How configuration flows: backend.toml defines backends, flake.nix defines artifacts
    - The MakeConfiguration is the central structure extracted from Nix evaluation

    Current content:
    pub mod backend;
    pub mod make;
    pub mod nix;

    Replace with module docs explaining the configuration architecture.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "config.*warning" || echo "No config warnings"
  </verify>
  <done>
    config/mod.rs has module-level documentation explaining configuration architecture
  </done>
</task>

<task type="auto">
  <name>Task 2: Document config/backend.rs</name>
  <files>pkgs/artifacts/src/config/backend.rs</files>
  <action>
    Add documentation to all public types and functions in src/config/backend.rs.

    Public types to document:
    - BackendSettings: HashMap wrapper for backend-specific settings
    - BackendCapabilities: Explicit capability declarations (shared, serializes)
    - BackendEntry: Complete backend definition with all script paths
    - BackendConfiguration: Container combining config and base paths

    Public functions to document:
    - BackendConfiguration::read_backend_config: Load and parse backend.toml with include support
    - BackendConfiguration::load_with_includes: Recursive loading with circular detection
    - BackendEntry::supports_shared: Check if backend supports shared artifacts

    Also add module-level docs explaining:
    - backend.toml format and structure
    - Include directive for splitting configuration
    - Backend capabilities (shared support, serialization)
    - Script paths (check_serialization, serialize, deserialize variants)

    Focus on the TOML structure and how includes work.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -c "missing_docs" || echo "0"
  </verify>
  <done>
    All public types and functions in backend.rs have complete documentation
  </done>
</task>

<task type="auto">
  <name>Task 3: Document config/make.rs</name>
  <files>pkgs/artifacts/src/config/make.rs</files>
  <action>
    Add documentation to the make configuration module.

    Public types to document:
    - FileDef: File definition with path, owner, group
    - PromptDef: Prompt definition with name and description
    - ArtifactDef: Complete artifact definition (name, shared, files, prompts, generator, serialization)
    - TargetType: Enum for Nixos vs HomeManager targets
    - GeneratorSource: Source of a generator (target + target_type)
    - GeneratorEntry: Entry in generators map
    - MakeConfiguration: Central configuration structure
    - ArtifactReference: Reference to an artifact in a specific target

    Public functions to document:
    - MakeConfiguration::extract_from_flake: Extract configuration from flake.nix
    - MakeConfiguration::collect_generators_for_target: Collect generators for a target
    - MakeConfiguration::get_backend_config_for: Get backend configuration for machine/user

    Module-level docs should explain:
    - Configuration is extracted from flake.nix via Nix evaluation
    - Supports both nixosConfigurations and homeConfigurations
    - Artifacts can be per-machine or shared across machines
    - Generator scripts produce files based on prompts

    Document the JSON structure expected from Nix evaluation.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "make.*warning" || echo "No warnings"
  </verify>
  <done>
    All public types in make.rs have field-level documentation
  </done>
</task>

<task type="auto">
  <name>Task 4: Document config/nix.rs</name>
  <files>pkgs/artifacts/src/config/nix.rs</files>
  <action>
    Add documentation to the Nix evaluation module.

    Public functions to document:
    - build_make_expr: Build Nix expression for extracting configuration
    - run_nix_eval: Run nix eval and parse JSON output

    Also document any internal helper functions that have complex logic.

    Module-level docs should explain:
    - This module handles Nix expression building and evaluation
    - Uses `nix eval` to extract artifact configurations from flake.nix
    - Builds a Nix expression that traverses nixosConfigurations and homeConfigurations
    - Parses the JSON output into Rust structs

    Document the Nix expression structure and how it extracts data.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "nix.*warning" || echo "No warnings"
  </verify>
  <done>
    All functions in nix.rs have documentation explaining Nix evaluation
  </done>
</task>

</tasks>

<verification>
- [ ] config/mod.rs has module-level documentation
- [ ] All public types in config modules have doc comments
- [ ] All public functions in config modules have doc comments
- [ ] BackendConfiguration and MakeConfiguration are fully documented
- [ ] cargo doc shows no missing_docs warnings for config module
</verification>

<success_criteria>
All config modules have comprehensive documentation: module-level docs explaining the configuration flow, type-level docs for all public structs/enums, and function-level docs for all public functions. The relationship between backend.toml and flake.nix is clearly explained.
</success_criteria>

<output>
After completion, create `.planning/phases/21-rust-documentation/21-03-SUMMARY.md`
</output>
