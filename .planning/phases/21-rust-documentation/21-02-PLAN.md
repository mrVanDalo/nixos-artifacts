---
phase: 21-rust-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/src/backend/mod.rs
  - pkgs/artifacts/src/backend/generator.rs
  - pkgs/artifacts/src/backend/serialization.rs
  - pkgs/artifacts/src/backend/helpers.rs
  - pkgs/artifacts/src/backend/output_capture.rs
  - pkgs/artifacts/src/backend/prompt.rs
  - pkgs/artifacts/src/backend/tempfile.rs
  - pkgs/artifacts/src/backend/temp_dir.rs
autonomous: true

must_haves:
  truths:
    - "All backend modules have module-level documentation"
    - "All public functions in backend have doc comments"
    - "All public types in backend have doc comments"
  artifacts:
    - path: "pkgs/artifacts/src/backend/mod.rs"
      provides: "Module-level documentation for backend module"
      min_lines: 15
    - path: "pkgs/artifacts/src/backend/generator.rs"
      provides: "Documentation for generator verification functions"
      exports: ["verify_generated_files"]
    - path: "pkgs/artifacts/src/backend/serialization.rs"
      provides: "Documentation for serialization operations"
      exports: ["CheckResult", "run_check_serialization", "run_serialize"]
  key_links:
    - from: "backend module docs"
      to: "backend/generator"
      via: "pub mod generator"
    - from: "backend module docs"
      to: "backend/serialization"
      via: "pub mod serialization"
---

<objective>
Add comprehensive documentation to the backend module and all its submodules.

Purpose: The backend module handles script execution, serialization, and file
generation. These are core operations that need clear documentation for
maintainers. Output: Documented backend module with all public APIs explained
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.planning/phases/21-rust-documentation/21-01-SUMMARY.md </execution_context>

<context>
The backend module contains:
- generator.rs: File generation verification
- serialization.rs: Backend script execution for check/serialize/deserialize
- helpers.rs: Utility functions for scripts
- output_capture.rs: Process output capture with timeout support
- prompt.rs: User prompt handling
- tempfile.rs: Temporary file utilities
- temp_dir.rs: Temporary directory management

Current state: Most functions have minimal or no documentation. Need
module-level docs and function-level docs for all public items.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document backend/mod.rs module</name>
  <files>pkgs/artifacts/src/backend/mod.rs</files>
  <action>
    Add comprehensive module-level documentation to src/backend/mod.rs.

    The documentation should explain:
    - What the backend module does (script execution, serialization, file operations)
    - Architecture: backend scripts are external programs called by the CLI
    - Key submodules: generator (file verification), serialization (backend scripts),
      helpers (script utilities), output_capture (process management)
    - Safety: All script execution uses bubblewrap containers for isolation

    Current content:
    pub mod generator;
    pub mod helpers;
    pub mod output_capture;
    pub mod prompt;
    pub mod serialization;
    pub mod tempfile;
    pub mod temp_dir;

    Replace with module docs explaining the backend architecture.

</action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "backend.*warning" || echo "No backend warnings"
    # Should show no backend-specific warnings
  </verify>
  <done>
    backend/mod.rs has module-level documentation explaining the backend architecture
  </done>
</task>

<task type="auto">
  <name>Task 2: Document backend/generator.rs</name>
  <files>pkgs/artifacts/src/backend/generator.rs</files>
  <action>
    Add documentation to all public functions in src/backend/generator.rs.

    Public functions to document:
    - verify_generated_files: Add description, parameters (# Arguments), return value (# Returns), and error conditions (# Errors)
    - run_generator_script: Add description, explain the bubblewrap container setup, document environment variables passed to generator

    Also add a module-level comment explaining:
    - Generator scripts produce files in a temporary output directory
    - This module verifies generators produce exactly the expected files
    - Uses bubblewrap for containerized execution

    Document any complex logic with inline comments explaining WHY not just WHAT.

</action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -c "missing_docs" || echo "0"
    # Should be 0
  </verify>
  <done>
    All public functions in generator.rs have doc comments with Arguments, Returns, and Errors sections
  </done>
</task>

<task type="auto">
  <name>Task 3: Document backend/serialization.rs</name>
  <files>pkgs/artifacts/src/backend/serialization.rs</files>
  <action>
    Add documentation to the serialization module.

    Public items to document:
    - CheckResult struct: Document fields (needs_generation, output) with clear descriptions
    - run_check_serialization: Explain it runs the check_serialization script, document exit code behavior (0 = up-to-date, non-zero = needs generation)
    - run_serialize: Explain it runs the serialize script to store generated files
    - run_shared_serialize: Explain shared artifact serialization with machines/users context
    - run_deserialize: Explain it runs the deserialize script to extract files

    Module-level docs should explain:
    - Serialization is how artifacts are stored and retrieved from backends
    - Three main operations: check, serialize, deserialize
    - Uses JSON files for configuration and temporary directories for staging
    - All operations have timeout protection

</action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "serialization.*warning" || echo "No warnings"
  </verify>
  <done>
    CheckResult and all serialization functions have complete documentation
  </done>
</task>

<task type="auto">
  <name>Task 4: Document remaining backend files</name>
  <files>
    pkgs/artifacts/src/backend/helpers.rs
    pkgs/artifacts/src/backend/output_capture.rs
    pkgs/artifacts/src/backend/prompt.rs
    pkgs/artifacts/src/backend/tempfile.rs
    pkgs/artifacts/src/backend/temp_dir.rs
  </files>
  <action>
    Add documentation to the remaining backend modules.

    helpers.rs:
    - Module docs: Script utility functions (path resolution, escaping, hashing)
    - Document public functions: resolve_path, escape_single_quoted, fnv1a64, validate_backend_script

    output_capture.rs:
    - Module docs: Process execution with output capture and timeout support
    - Document CapturedOutput struct and its fields
    - Document ScriptError enum variants
    - Document run_with_captured_output and run_with_captured_output_and_timeout

    prompt.rs:
    - Module docs: User prompt handling and value management
    - Document PromptValue enum and PromptValues type
    - Document prompt-related functions

    tempfile.rs:
    - Module docs: Temporary file utilities for artifact operations
    - Document TempFile struct and its methods

    temp_dir.rs:
    - Module docs: Temporary directory management
    - Document any public types/functions

    Keep documentation concise but complete. Focus on what each function does,
    what parameters it takes, what it returns, and when it might fail.

</action>
  <verify>
    cd pkgs/artifacts && cargo doc 2>&1 | grep -c "warning:" || echo "0"
    # Should be 0 or only unrelated warnings
  </verify>
  <done>
    All backend modules have complete documentation for public APIs
  </done>
</task>

</tasks>

<verification>
- [ ] backend/mod.rs has module-level documentation
- [ ] All public functions in backend modules have doc comments
- [ ] All public types (structs, enums) have doc comments
- [ ] cargo doc shows no missing_docs warnings for backend module
- [ ] Documentation includes examples where appropriate
- [ ] Complex logic has inline comments explaining "why"
</verification>

<success_criteria> All backend modules have comprehensive documentation:
module-level docs explaining the architecture, function-level docs with
Arguments/Returns/Errors sections, and inline comments for complex logic. cargo
doc generates without warnings. </success_criteria>

<output>
After completion, create `.planning/phases/21-rust-documentation/21-02-SUMMARY.md`
</output>
