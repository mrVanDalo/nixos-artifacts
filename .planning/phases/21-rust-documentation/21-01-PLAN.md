---
phase: 21-rust-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/src/logging.rs
  - pkgs/artifacts/src/tui/channels.rs
autonomous: true

must_haves:
  truths:
    - "cargo doc runs with zero warnings"
    - "All intra-doc links are valid"
    - "All HTML tag issues are resolved"
  artifacts:
    - path: "pkgs/artifacts/src/logging.rs"
      provides: "Fixed doc link escapes for [TIMESTAMP] and [LEVEL]"
      min_lines: 10
    - path: "pkgs/artifacts/src/tui/channels.rs"
      provides: "Fixed HTML tag warning for <String>"
      min_lines: 10
  key_links:
    - from: "logging.rs module docs"
      to: "cargo doc"
      via: "escaped brackets"
      pattern: "\\\[TIMESTAMP\\\]"
---

<objective>
Fix all `cargo doc` warnings to establish a clean documentation baseline.

Purpose: Before adding new documentation, we must eliminate existing warnings.
Output: Zero warnings from `cargo doc`
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.planning/STATE.md
@./.planning/phases/18-fix-compiler-clippy-warnings/18-05-SUMMARY.md
</execution_context>

<context>
Current `cargo doc` output shows 3 warnings:
1. Unresolved links to `TIMESTAMP` and `LEVEL` in src/logging.rs:7 (brackets interpreted as intra-doc links)
2. Unclosed HTML tag `String` in src/tui/channels.rs:145

These are simple formatting issues that must be fixed before comprehensive
documentation can be added.

Phase 18-05 already addressed many clippy documentation warnings. This plan
focuses on the remaining rustdoc-specific warnings.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix logging.rs doc link warnings</name>
  <files>pkgs/artifacts/src/logging.rs</files>
  <action>
    Fix the unresolved link warnings at line 7 in src/logging.rs.

    Current line:
    //! - Structured log format: [TIMESTAMP] [LEVEL] module: message

    Change to escape the brackets:
    //! - Structured log format: \[TIMESTAMP\] \[LEVEL\] module: message

    The brackets around TIMESTAMP and LEVEL are being interpreted as intra-doc links.
    Escaping them with backslashes tells rustdoc they are literal text.

    Also check if there are any other bracket patterns in module documentation that might be interpreted as links.

</action>
  <verify>
    cd pkgs/artifacts && cargo doc 2>&1 | grep -c "warning:" || echo "0"
    # Should show 2 (the channels.rs warning) or 0 (if both fixed)
  </verify>
  <done>
    Brackets in logging.rs are escaped and cargo doc no longer reports link warnings for logging.rs
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix channels.rs HTML tag warning</name>
  <files>pkgs/artifacts/src/tui/channels.rs</files>
  <action>
    Fix the unclosed HTML tag warning at line 145 in src/tui/channels.rs.

    Current line:
    /// for dispatch. Errors are encoded as bool+Option<String> rather than Result

    The `<String>` is being interpreted as an HTML tag. Fix by wrapping in backticks:
    /// for dispatch. Errors are encoded as `bool+Option<String>` rather than Result

    This is the recommended approach per the rustdoc help message.

</action>
  <verify>
    cd pkgs/artifacts && cargo doc 2>&1 | grep -c "warning:" || echo "0"
    # Should show 0 warnings
  </verify>
  <done>
    Option<String> is wrapped in backticks and cargo doc no longer reports HTML tag warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify clean cargo doc</name>
  <files></files>
  <action>
    Run a final verification to ensure `cargo doc` completes with zero warnings.

    Run: cd pkgs/artifacts && cargo doc 2>&1

    Capture the output and confirm there are no warning lines.

    Also verify that documentation is generated correctly by checking:
    - target/doc/artifacts/index.html exists
    - All expected modules are documented

</action>
  <verify>
    cd pkgs/artifacts && cargo doc 2>&1 | grep -E "^warning:" | wc -l
    # Should output 0
  </verify>
  <done>
    cargo doc completes with exactly 0 warnings
  </done>
</task>

</tasks>

<verification>
- [ ] cargo doc runs without any warnings
- [ ] Generated docs are viewable at target/doc/artifacts/index.html
- [ ] No broken intra-doc links remain
- [ ] No invalid HTML tag warnings remain
</verification>

<success_criteria> `cargo doc` completes with zero warnings, providing a clean
baseline for adding comprehensive documentation in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/21-rust-documentation/21-01-SUMMARY.md`
</output>
