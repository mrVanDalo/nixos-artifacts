---
phase: 21-rust-documentation
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - pkgs/artifacts/src/app/mod.rs
  - pkgs/artifacts/src/app/model.rs
  - pkgs/artifacts/src/app/message.rs
  - pkgs/artifacts/src/app/effect.rs
  - pkgs/artifacts/src/app/update.rs
  - pkgs/artifacts/src/cli/mod.rs
  - pkgs/artifacts/src/cli/args.rs
  - pkgs/artifacts/src/cli/headless.rs
autonomous: true

must_haves:
  truths:
    - "All app modules have complete documentation"
    - "All CLI modules have complete documentation"
    - "All public types in app/cli have doc comments"
  artifacts:
    - path: "pkgs/artifacts/src/app/mod.rs"
      provides: "Module-level documentation for app module"
      exports: ["Effect", "KeyEvent", "Msg", "Model", "Screen", "update"]
    - path: "pkgs/artifacts/src/app/model.rs"
      provides: "Documentation for Model, Screen, ArtifactStatus, etc."
      exports: ["Model", "Screen", "ArtifactEntry", "ArtifactStatus"]
    - path: "pkgs/artifacts/src/cli/mod.rs"
      provides: "Documentation for CLI entry point"
      exports: ["run"]
    - path: "pkgs/artifacts/src/cli/args.rs"
      provides: "Documentation for CLI arguments"
      exports: ["Cli"]
  key_links:
    - from: "app module docs"
      to: "app/model"
      via: "pub mod model"
    - from: "cli module docs"
      to: "cli/args"
      via: "pub mod args"
---

<objective>
Add comprehensive documentation to the app and cli modules.

Purpose: The app module implements the Elm Architecture (Model-Update-View), and cli module handles command-line interface. These are user-facing modules that need clear documentation for understanding application flow.
Output: Documented app and cli modules with all public APIs explained
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.planning/phases/21-rust-documentation/21-01-SUMMARY.md
</execution_context>

<context>
The app module contains the Elm Architecture implementation:
- model.rs: Application state (Model, Screen, ArtifactStatus, etc.)
- message.rs: Events (Msg, KeyEvent)
- effect.rs: Side effect descriptors (Effect)
- update.rs: Pure state transitions (update function)

The cli module contains:
- mod.rs: Main entry point (run function)
- args.rs: CLI argument definitions (using clap)
- headless.rs: Headless/scriptable execution mode

The tui module (already partially documented) contains:
- runtime.rs: Main event loop
- views/: UI rendering
- events.rs: Event handling

Current state: Some types already have excellent documentation (e.g., ArtifactStatus has lifecycle docs). Need to fill in gaps for modules and remaining types.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document app module files</name>
  <files>
    pkgs/artifacts/src/app/mod.rs
    pkgs/artifacts/src/app/model.rs
    pkgs/artifacts/src/app/message.rs
    pkgs/artifacts/src/app/effect.rs
    pkgs/artifacts/src/app/update.rs
  </files>
  <action>
    Add documentation to the app module (Elm Architecture).

    app/mod.rs:
    - Module-level docs explaining the Elm Architecture pattern
    - How Model-Update-View works in this codebase
    - Re-exports for convenience

    app/model.rs:
    - Model struct: Already has some docs, verify completeness
    - Screen enum: Document each variant
    - ArtifactEntry: Document fields
    - ArtifactStatus: Already well documented, verify completeness
    - ListEntry and variants: Document
    - GeneratingState, PromptState, etc.: Document purpose
    - StepLog, GenerationStep, etc.: Document

    app/message.rs:
    - Msg enum: Document each variant
    - KeyEvent struct: Document
    - Helper functions: Document

    app/effect.rs:
    - Effect enum: Document each variant (these are side effect descriptors)
    - Explain how effects are executed by the runtime

    app/update.rs:
    - init function: Document
    - update function: Document the pure state transition
    - Helper functions: Document

    Focus on the Elm Architecture: pure functions, immutable state, effects as data.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -c "missing_docs" || echo "0"
  </verify>
  <done>
    All app module public items have documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Document cli module files</name>
  <files>
    pkgs/artifacts/src/cli/mod.rs
    pkgs/artifacts/src/cli/args.rs
    pkgs/artifacts/src/cli/headless.rs
  </files>
  <action>
    Add documentation to the CLI module.

    cli/mod.rs:
    - Module-level docs explaining CLI structure
    - run function: Main entry point, document the flow
    - run_tui function: Document TUI initialization
    - resolve_flake_path: Document
    - resolve_backend_toml: Document environment variable and file lookup

    cli/args.rs:
    - Module-level docs for CLI arguments
    - Cli struct: Document the top-level command
    - Each field with #[arg]: Document the argument purpose
    - Commands enum: Document each subcommand
    - FilterArgs: Document filter options

    cli/headless.rs:
    - Module-level docs explaining headless mode
    - run_headless: Document non-interactive execution
    - ArtifactFilter: Document filter structure

    Include examples in documentation where helpful:
    - Example: artifacts tui --machine server-1
    - Example: artifacts --backend ./backends/agenix.toml
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "cli.*warning" || echo "No cli warnings"
  </verify>
  <done>
    All CLI arguments and commands have documentation
  </done>
</task>

<task type="auto">
  <name>Task 3: Document remaining tui module files</name>
  <files>
    pkgs/artifacts/src/tui/runtime.rs
    pkgs/artifacts/src/tui/events.rs
    pkgs/artifacts/src/tui/terminal.rs
    pkgs/artifacts/src/tui/model_builder.rs
    pkgs/artifacts/src/tui/background.rs
    pkgs/artifacts/src/tui/channels.rs
  </files>
  <action>
    Add documentation to remaining TUI module files.

    tui/runtime.rs:
    - run function: Document the main event loop
    - run_async: Document async variant
    - EffectHandler trait: Document
    - RunResult: Document

    tui/events.rs:
    - EventSource trait: Document
    - TerminalEventSource: Document
    - ScriptedEventSource: Document for testing

    tui/terminal.rs:
    - TerminalGuard: Document RAII terminal management
    - AppTerminal: Document type alias
    - install_panic_hook: Document

    tui/model_builder.rs:
    - build_model: Document
    - build_filtered_model: Document filtering
    - validate_model_capabilities: Document

    tui/background.rs:
    - BackgroundEffectHandler: Document
    - Document background job execution

    tui/channels.rs:
    - Command type: Document
    - Sender/Receiver types: Document
    - Effect channels: Document async communication

    Note: tui/mod.rs and tui/views/ already have documentation from previous phases.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "tui.*warning" || echo "No tui warnings"
  </verify>
  <done>
    All TUI modules have complete documentation
  </done>
</task>

</tasks>

<verification>
- [ ] app/mod.rs has module-level documentation
- [ ] All app types (Model, Screen, Msg, Effect) are documented
- [ ] cli/mod.rs and args.rs have complete documentation
- [ ] All CLI arguments have doc comments
- [ ] All tui modules have documentation
- [ ] cargo doc shows no missing_docs warnings for app/cli/tui
</verification>

<success_criteria>
All app, cli, and tui modules have comprehensive documentation. The Elm Architecture pattern is explained, CLI usage is documented with examples, and all TUI runtime components have clear documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/21-rust-documentation/21-04-SUMMARY.md`
</output>
