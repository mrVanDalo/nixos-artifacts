---
phase: 21-rust-documentation
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - pkgs/artifacts/src/lib.rs
  - pkgs/artifacts/src/macros.rs
  - pkgs/artifacts/src/effect_handler.rs
  - pkgs/artifacts/src/bin/artifacts.rs
autonomous: true

must_haves:
  truths:
    - "Crate root (lib.rs) has comprehensive documentation"
    - "All macros have documentation"
    - "Main binary has documentation"
    - "cargo doc runs with zero warnings"
  artifacts:
    - path: "pkgs/artifacts/src/lib.rs"
      provides: "Crate-level documentation with feature flags explained"
      min_lines: 30
    - path: "pkgs/artifacts/src/macros.rs"
      provides: "Documentation for string_vec! and logging macros"
      min_lines: 30
    - path: "pkgs/artifacts/src/bin/artifacts.rs"
      provides: "Binary entry point documentation"
      min_lines: 15
  key_links:
    - from: "lib.rs crate docs"
      to: "app module"
      via: "pub mod app"
    - from: "lib.rs crate docs"
      to: "cli module"
      via: "pub mod cli"
---

<objective>
Add documentation to the crate root, macros, main binary, and remaining files. Finalize documentation to achieve zero warnings.

Purpose: Complete the documentation effort by documenting the crate entry points and ensuring clean cargo doc output.
Output: Fully documented crate with zero cargo doc warnings
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.planning/phases/21-rust-documentation/21-01-SUMMARY.md
</execution_context>

<context>
Remaining files to document:
- lib.rs: Crate root with clippy allows and module exports (needs crate-level docs)
- macros.rs: Feature-gated logging macros and string_vec macro (already has some docs)
- effect_handler.rs: Legacy effect handler (needs module docs)
- bin/artifacts.rs: Main binary entry point (needs file-level docs)

Current lib.rs has extensive clippy allow comments but no crate documentation.
Current logging.rs already has good module docs (will be fixed in 21-01 for link warnings).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document crate root (lib.rs)</name>
  <files>pkgs/artifacts/src/lib.rs</files>
  <action>
    Add comprehensive crate-level documentation to src/lib.rs.

    The documentation should be inserted BEFORE the clippy allows (at the top).
    Use `//!` comments for crate-level documentation.

    Include:
    - Brief description of what artifacts-cli does
    - Core functionality: artifact generation, serialization, TUI
    - Architecture overview: app (Elm), backend (scripts), config (parsing)
    - Feature flags: "logging" feature enables file-based logging
    - Example usage: artifacts tui, artifacts generate
    - Key modules: app, backend, cli, config, tui

    Keep the existing clippy allow attributes - they document why we allow each lint.

    Example structure:
    //! # Artifacts CLI
    //!
    //! A Rust-based CLI for generating, serializing, and deserializing...
    //!
    //! ## Architecture
    //! ...
    //! ## Feature Flags
    //! ...
    //! ## Usage
    //! ...

    Also ensure the re-exports are documented:
    - Logger re-export needs a brief doc comment
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -c "missing_docs" || echo "0"
  </verify>
  <done>
    lib.rs has comprehensive crate-level documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Document macros.rs</name>
  <files>pkgs/artifacts/src/macros.rs</files>
  <action>
    Add documentation to the macros module.

    Current content has inline comments but no module-level docs or macro docs.

    Add at the top (before existing content):
    //! Utility macros for the artifacts-cli crate.
    //!
    //! This module provides:
    //! - string_vec![] - Convert literals to Vec<String>
    //! - log_debug!, log_trace!, log_error! - Feature-gated logging macros
    //!
    //! ## string_vec! Macro
    //! ...
    //!
    //! ## Logging Macros
    //! ...explain feature-gating...

    Replace the inline comment on string_vec! with proper macro documentation:
    /// Convert expressions to a Vec<String>.
    ///
    /// # Examples
    /// ```
    /// let names = string_vec!["alice", "bob"];
    /// ```

    Document the logging macros:
    /// Log a debug message (feature-gated, compiles to nothing when disabled).
    ///
    /// When the "logging" feature is enabled, this calls Logger::log().
    /// When disabled, this macro expands to nothing (zero-cost).

    Keep the _MACROS_RS const with its existing #[allow(dead_code)] and comment.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -E "macros.*warning" || echo "No warnings"
  </verify>
  <done>
    All macros have documentation with examples
  </done>
</task>

<task type="auto">
  <name>Task 3: Document main binary</name>
  <files>pkgs/artifacts/src/bin/artifacts.rs</files>
  <action>
    Add documentation to the main binary entry point.

    Add at the top of the file:
    //! Artifacts CLI - Main entry point
    //!
    //! This binary provides the `artifacts` command for managing NixOS artifacts.
    //! It supports interactive TUI mode, headless generation, and artifact listing.
    //!
    //! ## Commands
    //!
    //! - `artifacts` or `artifacts tui` - Launch interactive TUI
    //! - `artifacts list` - List all artifacts
    //! - `artifacts generate` - Generate artifacts in headless mode
    //!
    //! ## Configuration
    //!
    //! The CLI expects:
    //! - A `flake.nix` in the current directory or specified via --flake
    //! - A `backend.toml` defining serialization backends
    //!
    //! ## Exit Codes
    //!
    //! - 0: Success
    //! - 1: General error
    //! - 130: Interrupted (Ctrl+C)

    Add a brief doc comment to the main function:
    /// Main entry point for the artifacts CLI.
    ///
    /// Initializes logging, parses CLI arguments, and delegates to the appropriate runner.

    Also document any helper functions in the file.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc --no-deps 2>&1 | grep -c "warning:" || echo "0"
  </verify>
  <done>
    Main binary has file-level and function-level documentation
  </done>
</task>

<task type="auto">
  <name>Task 4: Final verification - zero warnings</name>
  <files></files>
  <action>
    Run final verification to ensure cargo doc completes with zero warnings.

    Steps:
    1. cd pkgs/artifacts
    2. Run: cargo doc 2>&1
    3. Capture full output
    4. Verify no warnings of any kind:
       - No missing_docs
       - No broken_intra_doc_links
       - No invalid_html_tags
       - No other rustdoc warnings

    Also verify:
    - Documentation is complete and renders correctly
    - All module documentation appears in the generated docs
    - Cross-references between modules work

    If any warnings remain, they must be fixed before this plan is complete.
  </action>
  <verify>
    cd pkgs/artifacts && cargo doc 2>&1 | grep -c "^warning:" || echo "0"
    # Must output exactly 0
  </verify>
  <done>
    cargo doc completes with exactly 0 warnings
  </done>
</task>

</tasks>

<verification>
- [ ] lib.rs has comprehensive crate-level documentation
- [ ] All modules are cross-referenced in crate docs
- [ ] Feature flags are documented
- [ ] macros.rs has module-level docs and macro docs with examples
- [ ] bin/artifacts.rs has file-level docs and main function docs
- [ ] cargo doc completes with exactly 0 warnings
- [ ] Generated docs render correctly and are complete
</verification>

<success_criteria>
The crate is fully documented: lib.rs has crate-level docs explaining the architecture, macros are documented with examples, the main binary is documented, and cargo doc runs with zero warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/21-rust-documentation/21-05-SUMMARY.md`
</output>
