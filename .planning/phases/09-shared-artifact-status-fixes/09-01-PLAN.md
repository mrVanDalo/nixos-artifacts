---
phase: 09-shared-artifact-status-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/src/tui/effect_handler.rs
  - pkgs/artifacts/src/backend/serialization.rs
  - pkgs/artifacts/src/config/make.rs
autonomous: true

must_haves:
  truths:
    - Shared artifacts transition from pending to correct status after check_serialization completes
    - Status reflects actual backend state (needs-generation, up-to-date, or failed)
    - Status never returns to pending once set
  artifacts:
    - path: pkgs/artifacts/src/tui/effect_handler.rs
      provides: SharedCheckSerialization effect handler
      contains: Effect::SharedCheckSerialization handling
    - path: pkgs/artifacts/src/backend/serialization.rs
      provides: run_shared_check_serialization function
      contains: shared_serialize script execution for check
  key_links:
    - from: tui/effect_handler.rs
      to: backend/serialization.rs
      via: run_shared_check_serialization call
      pattern: run_shared_check_serialization
---

<objective>
Implement shared artifact check serialization that correctly updates status from pending to final state.

Purpose: Currently shared artifacts remain stuck in "pending" status because the
SharedCheckSerialization effect is not handled. This plan implements the handler
to call the shared_check_serialization script and transition status correctly.

Output: Working shared artifact status updates via check_serialization script
execution
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./.planning/ROADMAP.md
@./.planning/phases/09-shared-artifact-status-fixes/09-CONTEXT.md
@./pkgs/artifacts/src/app/effect.rs
@./pkgs/artifacts/src/app/update.rs
@./pkgs/artifacts/src/tui/effect_handler.rs
@./pkgs/artifacts/src/backend/serialization.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared check serialization in backend</name>
  <files>pkgs/artifacts/src/backend/serialization.rs</files>
  <action>
Add `run_shared_check_serialization` function to `serialization.rs` that:
1. Takes artifact_name, backend_name, nixos_targets, home_targets, make config, and backend config
2. Validates the backend has shared_check_serialization script
3. Builds machines.json and users.json using existing helper functions (build_machines_json, build_users_json)
4. Runs the shared_check_serialization script with environment: $artifact, $machines, $users
5. Returns CheckResult similar to run_check_serialization
6. Handles timeout, IO errors, and script failures appropriately

Reference existing `run_shared_serialize` for structure - it already handles
machines/users JSON and shared script execution. The key difference is this only
checks, doesn't serialize.

Add error handling for missing shared_check_serialization script (backend
doesn't support shared artifacts).
</action>
<verify>cargo check passes with new function signature visible</verify>
<done>run_shared_check_serialization function exists and compiles, handles all
error cases</done>
</task>

<task type="auto">
  <name>Task 2: Add SharedCheckSerialization effect handler</name>
  <files>pkgs/artifacts/src/tui/effect_handler.rs</files>
  <action>
Add handler for `Effect::SharedCheckSerialization` in the `EffectHandler` implementation:
1. Extract shared artifact info from model using artifact_index
2. Call run_shared_check_serialization with:
   - artifact_name
   - backend_name from shared.info.backend_name
   - nixos_targets from shared.info.nixos_targets
   - home_targets from shared.info.home_targets
   - &self.make and &self.backend
3. Map the result to CheckSerializationResult message with artifact_index, result (Ok(needs_generation) or Err(message)), and captured output
4. Handle errors by returning Err result with descriptive message

Match the pattern used for Effect::CheckSerialization handler above it.
</action>
<verify>cargo check passes, no "non-exhaustive pattern" warnings for Effect
enum</verify>
<done>Effect::SharedCheckSerialization is fully handled in execute()
method</done>
</task>

<task type="auto">
  <name>Task 3: Export shared check function from backend module</name>
  <files>pkgs/artifacts/src/backend/mod.rs</files>
  <action>
Ensure run_shared_check_serialization is exported from the backend module:
1. Add `pub use serialization::run_shared_check_serialization;` to mod.rs if not already exported
2. Verify the function is accessible from effect_handler.rs via `crate::backend::serialization::`

The function should have this signature:

```rust
pub fn run_shared_check_serialization(
    artifact_name: &str,
    backend_name: &str,
    nixos_targets: &[String],
    home_targets: &[String],
    make: &MakeConfiguration,
    backend: &BackendConfiguration,
) -> Result<CheckResult>
```

</action>
  <verify>cargo check passes, function is accessible from effect_handler.rs</verify>
  <done>Function properly exported and accessible</done>
</task>

<task type="auto">
  <name>Task 4: Add tests for shared check serialization</name>
  <files>pkgs/artifacts/tests/backend/shared_check_tests.rs (new)</files>
  <action>
Create tests for run_shared_check_serialization:
1. Test success case - script returns exit 0 (up-to-date)
2. Test needs generation case - script returns non-zero exit (needs generation)
3. Test timeout case
4. Test missing script case (backend without shared support)

Use existing test patterns from serialization_tests.rs. Create mock backend.toml
with shared_check_serialization script for testing.

Test should verify:

- Correct CheckResult.needs_generation boolean based on exit code
- Captured output contains stdout/stderr from script
- Proper error when script missing
  </action>
  <verify>cargo test --lib passes all new tests</verify>
  <done>4+ tests covering success, needs-generation, timeout, and missing script
  cases</done>
  </task>

</tasks>

<verification>
After completion:
1. Run `cargo test --lib` - all tests pass
2. Run `cargo clippy` - no warnings
3. Verify shared artifacts in TUI:
   - Start TUI with shared artifact configuration
   - Watch status change from ○ (pending) to ◐ (needs-generation) or ✓ (up-to-date)
   - Status should not remain stuck on pending
4. Check logs show check_serialization script output for shared artifacts
</verification>

<success_criteria>

- SharedCheckSerialization effect is handled in effect_handler.rs
- run_shared_check_serialization function exists in serialization.rs
- Shared artifacts transition from Pending to NeedsGeneration, UpToDate, or
  Failed status
- Status never returns to Pending once set
- Tests verify correct behavior for all cases </success_criteria>

<output>
After completion, create `.planning/phases/09-shared-artifact-status-fixes/09-01-SUMMARY.md`
</output>
