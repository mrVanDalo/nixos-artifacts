---
phase: 20-unused-file-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/modules/ROOT/nav.adoc
  - docs/README.md
  - docs/CLAUDE.md
  - pkgs/artifacts/CLAUDE.md
  - README.md
  - docs/modules/ROOT/pages/*.adoc
  - docs/modules/ROOT/partials/*.adoc
autonomous: true

must_haves:
  truths:
    - All documentation files in docs/ are referenced in nav.adoc
    - No empty .adoc, .md, or .rs files exist
    - All CLAUDE.md files are current and useful
    - All README.md files are current and not orphaned
    - Documentation build output is clean
  artifacts:
    - path: "docs/modules/ROOT/nav.adoc"
      provides: "Antora navigation structure"
      min_lines: 20
    - path: "docs/README.md"
      provides: "Documentation build instructions"
      min_lines: 30
    - path: "docs/CLAUDE.md"
      provides: "AI guide for documentation"
      min_lines: 50
    - path: "pkgs/artifacts/CLAUDE.md"
      provides: "AI guide for CLI development"
      min_lines: 100
  key_links:
    - from: "docs/modules/ROOT/nav.adoc"
      to: "docs/modules/ROOT/pages/*.adoc"
      via: "include directives"
      pattern: "xref:.*.adoc"
---

<objective>
Audit and clean up orphaned documentation files, empty files, and unused documentation artifacts in the NixOS Artifacts Store codebase.

Purpose: Ensure documentation is maintainable, all files are referenced, and no
orphaned content exists that creates confusion.

Output: Clean documentation structure with all files properly referenced and no
empty/orphaned files.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@docs/modules/ROOT/nav.adoc @docs/README.md @docs/CLAUDE.md @README.md
@pkgs/artifacts/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit documentation files in docs/</name>
  <files>
    - docs/modules/ROOT/nav.adoc
    - docs/modules/ROOT/pages/*.adoc
    - docs/modules/ROOT/partials/*.adoc
  </files>
  <action>
    1. Read docs/modules/ROOT/nav.adoc to understand navigation structure
    2. List all .adoc files in docs/modules/ROOT/pages/
    3. List all .adoc files in docs/modules/ROOT/partials/
    4. Cross-reference: identify which page files are NOT referenced in nav.adoc
    5. Cross-reference: identify which partial files are NOT included in any page
    6. Check for empty .adoc files (files with only whitespace or comments)
    7. Document findings in comments at top of this task

    Expected findings to check:
    - nav.adoc should reference all meaningful pages
    - No pages should be orphaned
    - All partials should be included somewhere

</action>
  <verify>
    grep -r "xref:" docs/modules/ROOT/nav.adoc | wc -l
    ls docs/modules/ROOT/pages/*.adoc 2>/dev/null | wc -l
    ls docs/modules/ROOT/partials/*.adoc 2>/dev/null | wc -l
  </verify>
  <done>
    All documentation files catalogued, orphaned files identified, empty files noted
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit CLAUDE.md files for relevance</name>
  <files>
    - CLAUDE.md (project root)
    - docs/CLAUDE.md
    - pkgs/artifacts/CLAUDE.md
  </files>
  <action>
    1. Read all three CLAUDE.md files
    2. Check if root CLAUDE.md is referenced by any workflow or process
    3. Check if docs/CLAUDE.md is current (matches actual docs structure)
    4. Check if pkgs/artifacts/CLAUDE.md is current (matches actual code structure)
    5. Identify any outdated sections or missing information
    6. Note any CLAUDE.md files that should be consolidated or removed

    Key checks:
    - Root CLAUDE.md should reference all sub-project CLAUDE.md files
    - docs/CLAUDE.md should list actual pages in docs/
    - pkgs/artifacts/CLAUDE.md should match actual source structure

</action>
  <verify>
    wc -l CLAUDE.md docs/CLAUDE.md pkgs/artifacts/CLAUDE.md 2>/dev/null
    grep -l "CLAUDE.md" .opencode/get-shit-done/workflows/*.md 2>/dev/null
  </verify>
  <done>
    All CLAUDE.md files reviewed, outdated content identified, relevance confirmed
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit README.md files and remove orphaned documentation</name>
  <files>
    - README.md (project root)
    - docs/README.md
    - pkgs/artifacts/README.md (if exists)
  </files>
  <action>
    1. Read root README.md - verify it describes the project accurately
    2. Read docs/README.md - verify it describes documentation build process
    3. Check if pkgs/artifacts/README.md exists and is current
    4. Identify any README.md content that is:
       - Duplicated across files (should be consolidated)
       - Outdated (references removed features)
       - Orphaned (not linked from main documentation)
    5. Look for any other orphaned .md files in the repository root
    6. Update or remove orphaned files as appropriate

    Specific actions:
    - Root README.md should be the main project overview
    - docs/README.md should focus on documentation building
    - Remove any docs/ files not part of Antora build
    - Consolidate duplicated content

</action>
  <verify>
    ls *.md 2>/dev/null
    test -f docs/README.md && echo "docs/README.md exists"
    test -f pkgs/artifacts/README.md && echo "pkgs/artifacts/README.md exists"
  </verify>
  <done>
    All README.md files current and accurate, orphaned files removed or integrated
  </done>
</task>

</tasks>

<verification>
Overall phase checks:

1. FILE-01: Verify all docs/ files are referenced
   ```bash
   cd docs
   nix run .#build-docs 2>&1 | head -20
   ```

2. FILE-02: Verify no empty files
   ```bash
   find . -name "*.adoc" -o -name "*.md" | xargs -I {} sh -c 'test -s "{}" || echo "Empty: {}"'
   ```

3. FILE-03: Verify CLAUDE.md files are current
   ```bash
   head -50 CLAUDE.md
   head -50 docs/CLAUDE.md
   head -50 pkgs/artifacts/CLAUDE.md
   ```

4. FILE-04: Verify README.md files are current
   ```bash
   head -30 README.md
   head -30 docs/README.md
   ```

5. FILE-05: Verify no orphaned documentation
   ```bash
   ls -la docs/*.md docs/*.adoc 2>/dev/null || echo "No orphaned files in docs/"
   ```

</verification>

<success_criteria>

1. All documentation files in docs/ are either:
   - Referenced in nav.adoc (for pages)
   - Included in a page that IS in nav.adoc (for partials)
   - Documented as intentionally unreferenced with justification

2. No empty files exist:
   - All .adoc files have content beyond boilerplate
   - All .md files have meaningful content
   - All .rs files have actual code

3. All CLAUDE.md files are current:
   - Root CLAUDE.md references sub-project CLAUDE.md files
   - Each CLAUDE.md accurately describes its domain
   - No outdated references or broken links

4. All README.md files are current:
   - Root README.md describes the full project
   - docs/README.md focuses on documentation
   - No duplicated or outdated content

5. No orphaned documentation:
   - Files outside Antora structure are removed or integrated
   - Temporary or outdated files are deleted
   - Only intentionally kept files remain </success_criteria>

<output>
After completion, create `.planning/phases/20-unused-file-cleanup/20-01-SUMMARY.md`

Include:

- Files audited
- Issues found and fixed
- Files removed (if any)
- Files updated (if any)
- Verification results
  </output>
