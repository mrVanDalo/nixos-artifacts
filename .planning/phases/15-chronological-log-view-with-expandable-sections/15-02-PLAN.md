---
phase: 15-chronological-log-view-with-expandable-sections
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - pkgs/artifacts/src/tui/views/chronological_log.rs
  - pkgs/artifacts/src/tui/views/mod.rs
  - pkgs/artifacts/src/tui/views/list.rs
autonomous: true

must_haves:
  truths:
    - Chronological log view renders in a scrollable area
    - Each generation step (Check, Generate, Serialize) has a collapsible header
    - Expanded sections show all log lines with timestamps
    - Collapsed sections show summary line (line count, error count)
    - Visual indicators show expand/collapse state (+/- icons)
    - Current section is highlighted when navigating
  artifacts:
    - path: "pkgs/artifacts/src/tui/views/chronological_log.rs"
      provides: "Chronological log view implementation"
      exports: ["render_chronological_log"]
      min_lines: 200
    - path: "pkgs/artifacts/src/tui/views/mod.rs"
      provides: "View dispatcher updated"
    - path: "pkgs/artifacts/src/tui/views/list.rs"
      provides: "Navigation from list to log view"
  key_links:
    - from: "src/tui/views/chronological_log.rs"
      to: "src/app/model.rs"
      via: "ChronologicalLogState reads"
    - from: "src/tui/views/list.rs"
      to: "src/tui/views/chronological_log.rs"
      via: "Enter key on artifact opens log view"
---

<objective>
Create the chronological log view UI with expandable sections for each generation step.

Purpose: Display logs chronologically with collapsible sections per step,
allowing users to focus on relevant output.

Output: Render function for chronological log view with expandable sections.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@pkgs/artifacts/src/tui/views/list.rs
@pkgs/artifacts/src/tui/views/progress.rs
@pkgs/artifacts/src/app/model.rs

The view layer uses ratatui widgets. Follow existing patterns in list.rs and
progress.rs. Key widgets: Block, List, Paragraph, Scrollbar. LogEntry has level
(Info, Output, Error, Success) for styling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chronological_log.rs view module</name>
  <files>pkgs/artifacts/src/tui/views/chronological_log.rs</files>
  <action>
Create new view file for chronological log display:

1. Create `pkgs/artifacts/src/tui/views/chronological_log.rs`

2. Implement `render_chronological_log` function:

```rust
pub fn render_chronological_log(
    frame: &mut Frame,
    model: &Model,
    state: &ChronologicalLogState,
    area: Rect,
)
```

3. Layout structure:
   - Top: Title bar showing "Logs for: {artifact_name}" with help text
   - Middle: Scrollable log content area
   - Bottom: Status bar with keybindings (Esc=back, Space=toggle, +/-
     expand/collapse)

4. For each LogStep (Check, Generate, Serialize), render:
   - Header row with:
     - Expand/collapse indicator (+/- or ▼/▶)
     - Step name ("Check", "Generate", "Serialize")
     - Summary when collapsed: "{N} lines, {E} errors"
     - Focus indicator when section is focused
   - Content area (only when expanded):
     - Chronological list of LogEntry items for that step
     - Styled by LogLevel (Info=gray, Output=default, Error=red, Success=green)
     - Timestamps if available

5. Implement helper functions:
   - `render_section_header` - renders collapsible header
   - `render_section_content` - renders log lines when expanded
   - `count_lines_and_errors` - returns (total_lines, error_count) for summary
   - `log_level_style` - returns Style for each LogLevel

6. Use Scrollbar widget for vertical scrolling support.

Follow existing view patterns from progress.rs and list.rs.
</action>
<verify>cargo check --lib passes with no errors</verify>
<done>chronological_log.rs created with render function, header/content
rendering, and styling</done>
</task>

<task type="auto">
  <name>Task 2: Add view to module exports</name>
  <files>pkgs/artifacts/src/tui/views/mod.rs</files>
  <action>
Wire up the new view module:

1. Add `pub mod chronological_log;` to module declarations

2. Add to `render()` function's match statement:

```rust
Screen::ChronologicalLog(ref state) => {
    chronological_log::render_chronological_log(frame, model, state, area);
}
```

3. Import the module at top of file if needed.
   </action>
   <verify>cargo check --lib passes with no errors</verify>
   <done>Module wired, render dispatcher updated</done>
   </task>

<task type="auto">
  <name>Task 3: Add navigation from list to log view</name>
  <files>pkgs/artifacts/src/tui/views/list.rs, pkgs/artifacts/src/app/update.rs</files>
  <action>
Add keybinding to open chronological log from artifact list:

1. In `list.rs` keybinding help text (render_legend function), add:
   - "l - view logs" or show in existing help

2. In `update.rs`, add handler for 'l' key in ArtifactList screen:

```rust
(KeyCode::Char('l'), Screen::ArtifactList) => {
    if let Some(entry) = model.entries.get(model.selected_index) {
        let state = ChronologicalLogState::new(
            model.selected_index,
            entry.artifact_name().to_string(),
        );
        // Pre-populate expanded_sections from model
        model.screen = Screen::ChronologicalLog(state);
    }
    Effect::None
}
```

3. Ensure ChronologicalLogState::new() constructor exists in model.rs:
   - Takes artifact_index, artifact_name
   - Initializes expanded_sections with all steps expanded by default
   - Sets scroll_offset to 0
   - Sets focused_section to Some(LogStep::Check)

4. Also handle Enter key on already-generated artifacts to open log view instead
   of regenerating.
   </action>
   <verify>cargo check --lib passes, existing tests still pass</verify>
   <done>'l' key opens log view from list, Enter on Done artifacts opens
   logs</done>
   </task>

</tasks>

<verification>
- cargo check --lib compiles without errors
- New view module integrates with existing view dispatcher
- Keyboard navigation from list works
- View renders without panics
</verification>

<success_criteria> Chronological log view:

- Renders with proper layout (title, content, status bar)
- Shows all three sections (Check, Generate, Serialize)
- Displays expand/collapse indicators
- Shows summary when collapsed
- Scrolls vertically when content overflows
- Returns to list on Esc </success_criteria>

<output>
After completion, create `.planning/phases/15-chronological-log-view-with-expandable-sections/15-02-SUMMARY.md`
</output>
