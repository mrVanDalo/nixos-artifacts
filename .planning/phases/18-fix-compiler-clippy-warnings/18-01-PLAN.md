---
phase: 18-fix-compiler-clippy-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/src/effect_handler.rs
  - pkgs/artifacts/src/logging.rs
  - pkgs/artifacts/src/config/make.rs
  - pkgs/artifacts/src/config/nix.rs
  - pkgs/artifacts/src/tui/views/regenerate_dialog.rs
  - pkgs/artifacts/src/tui/views/chronological_log.rs
  - pkgs/artifacts/src/tui/views/generator_selection.rs
  - pkgs/artifacts/src/tui/background.rs
  - pkgs/artifacts/src/backend/generator.rs
  - pkgs/artifacts/src/backend/serialization.rs
autonomous: true

must_haves:
  truths:
    - "Main code compiles with zero rustc warnings"
    - "All unused imports are removed or used"
    - "All unused variables are prefixed or used"
  artifacts:
    - path: "pkgs/artifacts/src/effect_handler.rs"
      provides: "Cleaned unused imports"
    - path: "pkgs/artifacts/src/logging.rs"
      provides: "Cleaned unused imports and variables"
    - path: "pkgs/artifacts/src/config/make.rs"
      provides: "Cleaned unused imports and variables"
    - path: "pkgs/artifacts/src/config/nix.rs"
      provides: "Cleaned unused imports and variables"
    - path: "pkgs/artifacts/src/tui/views/regenerate_dialog.rs"
      provides: "Cleaned unused imports"
    - path: "pkgs/artifacts/src/tui/views/chronological_log.rs"
      provides: "Cleaned unused imports"
    - path: "pkgs/artifacts/src/tui/views/generator_selection.rs"
      provides: "Cleaned unused imports and unused variable"
    - path: "pkgs/artifacts/src/tui/background.rs"
      provides: "Cleaned unused imports and variables"
    - path: "pkgs/artifacts/src/backend/generator.rs"
      provides: "Cleaned unused variable"
    - path: "pkgs/artifacts/src/backend/serialization.rs"
      provides: "Cleaned unused parameters and variables"
  key_links:
    - from: "cargo build"
      to: "Zero warnings"
      pattern: "Finished dev.*with 0 warnings"
---

<objective>
Fix all rustc compiler warnings in the main codebase. This includes unused imports, unused variables, and unused assignments.

Purpose: Satisfy LINT-01 (main code compiles with zero compiler warnings). Output: Cleaned source files with all compiler warnings resolved.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

## Current Warnings (from cargo build)

### Unused Imports (11 total)
1. `src/effect_handler.rs:24` - `use std::collections::HashMap;`
2. `src/effect_handler.rs:30` - `use crate::app::model::OutputStream;`
3. `src/effect_handler.rs:31` - `ScriptOutput` from `crate::tui::channels`
4. `src/logging.rs:35` - `use std::io::Write;`
5. `src/config/make.rs:1` - `use crate::log_trace;`
6. `src/config/nix.rs:2` - `use crate::log_debug;`
7. `src/config/nix.rs:3` - `use crate::log_trace;`
8. `src/tui/views/regenerate_dialog.rs:6` - `Wrap` from ratatui
9. `src/tui/views/chronological_log.rs:6` - `Scrollbar` and `ScrollbarOrientation`
10. `src/tui/views/generator_selection.rs:5` - `Stylize` from ratatui
11. `src/tui/background.rs:23` - `use tokio::io::AsyncBufReadExt;`

### Unused Variables (9 total)
1. `src/backend/generator.rs:123` - `bwrap_pretty` - prefix with `_`
2. `src/backend/serialization.rs:36` - `artifact_name` parameter - prefix with `_`
3. `src/backend/serialization.rs:518` - `target_label` - prefix with `_`
4. `src/logging.rs:309` - `msg` parameter in `log()` - prefix with `_`
5. `src/logging.rs:324` - `component` parameter - prefix with `_`
6. `src/logging.rs:324` - `msg` parameter - prefix with `_`
7. `src/config/make.rs:182` - `pretty` - prefix with `_`
8. `src/config/nix.rs:26` - `pretty` - prefix with `_`
9. `src/tui/background.rs:104` - `target_for_error` - prefix with `_`
10. `src/tui/background.rs:676` - `machine_targets` - use `_machine_targets` pattern
11. `src/tui/views/generator_selection.rs:271` - `visual_idx` - completely unused, can remove

### Pattern
For unused parameters in public functions: use `_name` prefix
For unused variables: use `_name` prefix or remove if truly unused
For unused imports: remove entirely
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix unused imports in core modules</name>
  <files>
    pkgs/artifacts/src/effect_handler.rs
    pkgs/artifacts/src/logging.rs
    pkgs/artifacts/src/config/make.rs
    pkgs/artifacts/src/config/nix.rs
  </files>
  <action>
Remove the following unused imports from each file:

**src/effect_handler.rs (lines 24, 30, 31):**
- Remove: `use std::collections::HashMap;`
- Remove: `use crate::app::model::OutputStream;`
- Remove: `ScriptOutput` from the import on line 31 (keep EffectCommand, EffectResult)

**src/logging.rs (line 35):**
- Remove: `use std::io::Write;`

**src/config/make.rs (line 1):**
- Remove: `use crate::log_trace;`

**src/config/nix.rs (lines 2-3):**
- Remove: `use crate::log_debug;`
- Remove: `use crate::log_trace;`

Note: Do NOT remove other imports that are used. Only remove the specific unused ones listed.
  </action>
  <verify>cd pkgs/artifacts && cargo build 2>&1 | grep -c "unused import" | xargs -I {} test {} -eq 0 || echo "Still has unused import warnings"</verify>
  <done>No "unused import" warnings from rustc for these four files</done>
</task>

<task type="auto">
  <name>Task 2: Fix unused imports in TUI views</name>
  <files>
    pkgs/artifacts/src/tui/views/regenerate_dialog.rs
    pkgs/artifacts/src/tui/views/chronological_log.rs
    pkgs/artifacts/src/tui/views/generator_selection.rs
    pkgs/artifacts/src/tui/background.rs
  </files>
  <action>
Remove unused imports and variables:

**src/tui/views/regenerate_dialog.rs (line 6):**
- Remove `Wrap` from the ratatui widgets import

**src/tui/views/chronological_log.rs (line 6):**
- Remove `Scrollbar` and `ScrollbarOrientation` from the ratatui widgets import

**src/tui/views/generator_selection.rs (line 5):**
- Remove `Stylize` from the ratatui style import

**src/tui/background.rs (line 23):**
- Remove `use tokio::io::AsyncBufReadExt;`
  </action>
  <verify>cd pkgs/artifacts && cargo build 2>&1 | grep -E "src/tui.*unused import" | wc -l | xargs -I {} test {} -eq 0</verify>
  <done>No "unused import" warnings from rustc for TUI modules</done>
</task>

<task type="auto">
  <name>Task 3: Fix unused variables and assignments</name>
  <files>
    pkgs/artifacts/src/backend/generator.rs
    pkgs/artifacts/src/backend/serialization.rs
    pkgs/artifacts/src/logging.rs
    pkgs/artifacts/src/config/make.rs
    pkgs/artifacts/src/config/nix.rs
    pkgs/artifacts/src/tui/background.rs
    pkgs/artifacts/src/tui/views/generator_selection.rs
  </files>
  <action>
Fix unused variables by either prefixing with underscore or removing:

**src/backend/generator.rs (line 123):**
- Change `let bwrap_pretty = {` to `let _bwrap_pretty = {`

**src/backend/serialization.rs:**
- Line 36: Change `artifact_name: &str,` to `_artifact_name: &str,`
- Line 518: Change `let target_label = ...` to `let _target_label = ...`

**src/logging.rs:**
- Line 309: Change `pub fn log(msg: &str)` to `pub fn log(_msg: &str)`
- Line 324: Change `pub fn log_component(component: &str, msg: &str)` to `pub fn log_component(_component: &str, _msg: &str)`

**src/config/make.rs (line 182):**
- Change `let pretty = ...` to `let _pretty = ...`

**src/config/nix.rs (line 26):**
- Change `let pretty = ...` to `let _pretty = ...`

**src/tui/background.rs:**
- Line 104: Change `let target_for_error = ...` to `let _target_for_error = ...`
- Line 676: Change pattern match to use `_machine_targets: _`

**src/tui/views/generator_selection.rs (lines 271-276):**
- Remove the entire `visual_idx` variable and its increments (it's completely unused)
  </action>
  <verify>cd pkgs/artifacts && cargo build 2>&1 | grep -E "(unused variable|unused_assignments)" | wc -l | xargs -I {} test {} -eq 0</verify>
  <done>No "unused variable" or "unused_assignments" warnings from rustc</done>
</task>

</tasks>

<verification>
Run these commands to verify LINT-01 is satisfied:

```bash
cd pkgs/artifacts
cargo build 2>&1 | grep -E "warning|error" | wc -l
```

Expected: 0 warnings and 0 errors

Alternative check to see full output:
```bash
cargo build
```
Should complete with: "Finished dev [unoptimized + debuginfo] target(s) in Xs"
</verification>

<success_criteria>
1. `cargo build` completes with zero warnings
2. All unused imports removed from identified files
3. All unused variables prefixed with underscore
4. `visual_idx` in generator_selection.rs completely removed (not just prefixed)
</success_criteria>

<output>
After completion, create `.planning/phases/18-fix-compiler-clippy-warnings/18-01-SUMMARY.md`

Expected: Clean build with zero rustc warnings for main code.
</output>
