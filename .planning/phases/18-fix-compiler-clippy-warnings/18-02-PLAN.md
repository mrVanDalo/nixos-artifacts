---
phase: 18-fix-compiler-clippy-warnings
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Main code passes clippy with zero warnings at default level"
    - "clippy defaults are clean after rustc warnings are fixed"
  artifacts:
    - path: "pkgs/artifacts/src/"
      provides: "Clean clippy output"
  key_links:
    - from: "cargo clippy"
      to: "Zero warnings"
      pattern: "Finished dev.*with 0 warnings"
---

<objective>
Verify and ensure `cargo clippy` passes with zero warnings at the default lint level on the main codebase.

Purpose: Satisfy LINT-02 (main code passes clippy with zero warnings). Output: Confirmation that clippy passes with no warnings.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

## Clippy Status

Clippy shares many warnings with rustc (unused imports, unused variables). After Plan 01 fixes all rustc warnings, clippy at default level should also be clean.

Current observation: clippy output is essentially the same as rustc warnings for this codebase, which are all "unused" warnings.

**Important:** This plan DEPENDS on Plan 01 (18-01) which fixes all rustc warnings. The warnings clippy reports will be resolved by Plan 01's changes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify clippy passes at default level</name>
  <files>
    pkgs/artifacts/Cargo.toml
  </files>
  <action>
Run `cargo clippy` and verify it completes with zero warnings.

**Prerequisite:** Plan 01 must be complete (all rustc warnings fixed first).

**Command:**
```bash
cd pkgs/artifacts
cargo clippy 2>&1 | grep -E "^warning:" | wc -l
```

**Expected:** Output should be 0 (or show only the same warnings as rustc, which should already be fixed).

If any clippy-specific warnings appear (different from rustc "unused" warnings):
1. Read the specific warning message
2. Apply the suggested fix
3. Re-run clippy to verify

**Note:** Most clippy warnings at default level are already covered by rustc "unused" lints. Any unique clippy warnings would be for style/performance issues.
  </action>
  <verify>cd pkgs/artifacts && cargo clippy 2>&1 | grep -c "warning:" | xargs -I {} test {} -eq 0 || echo "Warnings found"</verify>
  <done>`cargo clippy` completes with zero warnings at default lint level</done>
</task>

</tasks>

<verification>
Run these commands to verify LINT-02 is satisfied:

```bash
cd pkgs/artifacts
cargo clippy 2>&1 | grep -E "warning|error" | wc -l
```

Expected: 0 warnings and 0 errors

Alternative check to see full output:
```bash
cargo clippy
```
Should complete with: "Finished dev [unoptimized + debuginfo] target(s) in Xs"
With no warning lines.
</verification>

<success_criteria>
1. `cargo clippy` completes with zero warnings
2. All clippy lint categories at default level pass
3. No additional manual fixes needed beyond Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/18-fix-compiler-clippy-warnings/18-02-SUMMARY.md`

Expected: Clippy passes with zero warnings after rustc warnings are fixed.
</output>
