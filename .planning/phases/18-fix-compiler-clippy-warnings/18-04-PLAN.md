---
phase: 18-fix-compiler-clippy-warnings
plan: 04
type: execute
wave: 4
depends_on: ["18-02", "18-03"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Tests pass clippy with zero warnings at default level"
    - "clippy --tests passes cleanly"
  artifacts:
    - path: "pkgs/artifacts/tests/"
      provides: "Clean clippy output for tests"
  key_links:
    - from: "cargo clippy --tests"
      to: "Zero warnings"
      pattern: "Finished test.*with 0 warnings"
---

<objective>
Verify and ensure `cargo clippy --tests` passes with zero warnings at the default lint level.

Purpose: Satisfy LINT-04 (tests pass clippy with zero warnings). Output: Confirmation that clippy on tests passes with no warnings.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

## Clippy on Tests Status

Clippy --tests will check both main code and test code. After Plan 01 (rustc main code warnings) and Plan 03 (rustc test warnings), clippy should also be clean.

**Dependencies:**
- Plan 02: Main code clippy clean
- Plan 03: Test code rustc warnings fixed

**Observation:** Clippy at default level shares most warnings with rustc. Since Plans 01-03 fix all rustc warnings, clippy should pass cleanly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify clippy --tests passes</name>
  <files>
    pkgs/artifacts/tests/
  </files>
  <action>
Run `cargo clippy --tests` and verify it completes with zero warnings.

**Prerequisites:**
- Plan 01 complete (main code rustc warnings fixed)
- Plan 02 complete (main code clippy clean)
- Plan 03 complete (test code rustc warnings fixed)

**Command:**
```bash
cd pkgs/artifacts
cargo clippy --tests 2>&1 | grep -E "^warning:" | wc -l
```

**Expected:** Output should be 0.

If any clippy-specific warnings appear:
1. Read the specific warning message
2. Apply the suggested fix (clippy usually provides very specific suggestions)
3. Re-run clippy --tests to verify

**Common clippy warnings in tests:**
- `unwrap()` used in tests (clippy may warn, but this is often acceptable in tests)
- Large `enum` variants
- Needless borrows (`&*foo` instead of `foo`)
- Single-match for if-let

For test code, some warnings like `unwrap()` are acceptable. You can add `#![allow(clippy::unwrap_used)]` at the top of test files if needed.
  </action>
  <verify>cd pkgs/artifacts && cargo clippy --tests 2>&1 | grep -c "warning:" | xargs -I {} test {} -eq 0 || echo "Warnings found"</verify>
  <done>`cargo clippy --tests` completes with zero warnings</done>
</task>

</tasks>

<verification>
Run these commands to verify LINT-04 is satisfied:

```bash
cd pkgs/artifacts
cargo clippy --tests 2>&1 | grep -E "warning|error" | wc -l
```

Expected: 0 warnings and 0 errors

Alternative check:
```bash
cargo clippy --tests
```
Should complete with: "Finished test [unoptimized + debuginfo] target(s) in Xs"
With no warning lines.
</verification>

<success_criteria>
1. `cargo clippy --tests` completes with zero warnings
2. All lint categories at default level pass on test code
3. No additional manual fixes needed beyond Plans 01-03
</success_criteria>

<output>
After completion, create `.planning/phases/18-fix-compiler-clippy-warnings/18-04-SUMMARY.md`

Expected: Clippy on tests passes with zero warnings after rustc warnings are fixed.
</output>
