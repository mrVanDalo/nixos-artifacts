---
phase: 05-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/tests/tui/integration_tests.rs
  - pkgs/artifacts/tests/cli/
  - pkgs/artifacts/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "Existing integration tests still pass with async runtime (run_async)"
    - "TUI snapshot tests remain unchanged and functional"
    - "Insta snapshots provide readable test output"
    - "CLI tests verify end-to-end flows with async architecture"
  artifacts:
    - path: "pkgs/artifacts/tests/tui/integration_tests.rs"
      provides: "Updated integration tests using run_async where needed"
      min_lines: 600
    - path: "pkgs/artifacts/tests/cli/integration_tests.rs"
      provides: "CLI command integration tests"
      min_lines: 100
  key_links:
    - from: "tests/tui/integration_tests.rs"
      to: "src/tui/runtime.rs"
      via: "run() or run_async() function"
      pattern: "run\(|run_async\("
    - from: "tests/cli/integration_tests.rs"
      to: "src/bin/artifacts.rs"
      via: "CLI command execution"
      pattern: "assert_cmd_snapshot!"
---

<objective>
Update existing integration tests to work with the async channel-based architecture. Focus on ensuring tests use the correct runtime (run vs run_async), verify insta snapshot tests remain functional, and add CLI-level integration tests for end-to-end verification. Per user decision: integration tests should focus on insta snapshots and avoid assertion chains where possible.

Purpose: Ensure end-to-end flows work correctly with async architecture while
maintaining the snapshot-based testing approach. Output: Updated
integration_tests.rs and new CLI integration tests.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@./CLAUDE.md
@./pkgs/artifacts/CLAUDE.md
@./pkgs/artifacts/tests/tui/integration_tests.rs
@./pkgs/artifacts/src/tui/runtime.rs
@./pkgs/artifacts/src/tui/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Analyze and update TUI integration tests for async runtime</name>
  <files>pkgs/artifacts/tests/tui/integration_tests.rs</files>
  <action>
    Review existing integration_tests.rs and update for async architecture:

    1. Analyze current test structure:
       - Tests use run() function (sync version) via test_helpers::run_tui()
       - Tests rely on ScriptedEventSource for deterministic event sequences
       - Snapshots capture ModelState before/after execution

    2. Determine if updates needed:
       - The sync run() function in runtime.rs is still available and functional
       - Integration tests may continue using run() if they don't need real effects
       - For tests requiring real async effects, migrate to run_async()

    3. Update run_tui helper:
       - Check if run_tui uses run() or run_async()
       - If using run() and tests pass, document as intentional (no real effects needed)
       - If async effects needed, create run_tui_async variant using run_async()

    4. Update tests as needed:
       - Most tests use test backend (no real effects) - keep using run()
       - For tests with real backend effects, switch to run_async()
       - Maintain existing #[serial] attributes for test isolation

    5. Per user decision: Focus on insta snapshots, avoid assertion chains
       - Keep existing snapshot assertions
       - Remove any verbose assertion chains if present
       - Ensure snapshots capture meaningful state differences

    6. Run all existing integration tests to verify they pass:
       - cargo test --test tests tui::integration_tests
       - If failures, diagnose and fix (likely async-related timing)

</action>
  <verify>
    Run `cargo test --test tests tui::integration_tests` and verify all tests pass
  </verify>
  <done>
    Integration tests updated and all passing:
    - Tests either use run() (intentionally, no real effects) or run_async()
    - All existing scenarios pass: single-artifact-with-prompts, two-artifacts, multiple-machines, etc.
    - Snapshots remain the primary assertion mechanism
    - #[serial] attributes present for test isolation
  </done>
</task>

<task type="auto">
  <name>Create CLI-level integration tests</name>
  <files>pkgs/artifacts/tests/cli/integration_tests.rs</files>
  <action>
    Create CLI integration tests using insta-cmd pattern for end-to-end verification:

    1. Create tests/cli/integration_tests.rs module:
       ```rust
       use insta_cmd::assert_cmd_snapshot;
       use insta_cmd::get_cargo_bin;
       use std::process::Command;
       use serial_test::serial;
       
       fn cli() -> Command {
           Command::new(get_cargo_bin("artifacts"))
       }
       ```

    2. Implement tests covering CLI commands:
       - test_cli_help: Verify help output via insta snapshot
       - test_cli_version: Verify version flag works
       - test_cli_generate_no_args: Error when run without flake
       - test_cli_generate_with_flake: Generate in example directory
       - test_cli_list: List artifacts in example directory

    3. Use existing example scenarios for test data:
       - single-artifact-with-prompts for generate tests
       - two-artifacts-no-prompts for list tests
       - Point to examples/scenarios/ directories

    4. Per user decision: Focus on insta snapshots
       - Use assert_cmd_snapshot! for all CLI output
       - No assertion chains - single snapshot per test
       - Capture stdout, stderr, and exit code

    5. Add to tests/cli/mod.rs:
       - mod integration_tests;

    6. All tests marked with #[serial] to prevent parallel execution

</action>
  <verify>
    Run `cargo test --test tests cli::integration_tests` and verify all tests pass
  </verify>
  <done>
    CLI integration tests created:
    - tests/cli/integration_tests.rs exists with 4+ test functions
    - Tests use insta-cmd snapshot pattern
    - All tests use #[serial] attribute
    - Tests cover help, version, generate, and list commands
    - Snapshots captured for all test outputs
  </done>
</task>

<task type="auto">
  <name>Verify view tests remain unchanged</name>
  <files>pkgs/artifacts/tests/tui/view_tests.rs</files>
  <action>
    Per user decision: view tests should remain unchanged. Verify:

    1. Check view_tests.rs exists and compiles:
       - Run `cargo test --test tests tui::view_tests`
       - If compilation errors, fix (should be independent of async changes)

    2. Verify view tests don't need async runtime:
       - View tests render Model to TestBackend
       - Pure function testing, no effects needed
       - Should work unchanged

    3. Document in summary:
       - View tests intentionally unchanged per user decision
       - They test pure view rendering, not async behavior
       - Snapshots remain valid

    4. Run view tests to confirm:
       - cargo test --test tests tui::view_tests
       - All tests should pass without modification

</action>
  <verify>
    Run `cargo test --test tests tui::view_tests` and verify all tests pass without changes
  </verify>
  <done>
    View tests verified:
    - view_tests.rs compiles and passes without changes
    - All existing view snapshots remain valid
    - Documented that view tests were intentionally left unchanged per user decision
  </done>
</task>

</tasks>

<verification>
1. Run `cargo test --test tests` - all integration tests pass
2. Verify TUI integration tests use appropriate runtime (run or run_async)
3. Confirm CLI integration tests exist with insta-cmd snapshots
4. Check view tests pass unchanged
5. Ensure all tests use #[serial] for sequential execution
6. Verify no assertion chains - primary assertions are insta snapshots
</verification>

<success_criteria>

- TUI integration_tests.rs updated and all tests pass
- CLI integration tests created in tests/cli/integration_tests.rs
- View tests verified unchanged and passing
- All tests use #[serial] for sequential execution
- Insta snapshots are primary assertion mechanism
- No verbose assertion chains
- All existing scenario tests pass (single-artifact, two-artifacts,
  multiple-machines, etc.) </success_criteria>

<output>
After completion, create `.planning/phases/05-validation/05-03-SUMMARY.md`
</output>
