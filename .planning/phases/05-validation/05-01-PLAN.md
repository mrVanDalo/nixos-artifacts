---
phase: 05-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/tests/async_tests/state_machine_tests.rs
  - pkgs/artifacts/tests/async_tests/mod.rs
  - pkgs/artifacts/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "State machine transitions from Pending → Running → Success are testable"
    - "Commands sent to background task match expected EffectCommand variants"
    - "Final Model state reflects successful async effect completion"
    - "State machine handles failure transitions (Pending → Running → Failed)"
  artifacts:
    - path: "pkgs/artifacts/tests/async_tests/state_machine_tests.rs"
      provides: "State machine simulation tests with dual assertion strategy"
      min_lines: 250
    - path: "pkgs/artifacts/tests/async_tests/mod.rs"
      provides: "Module exports for state_machine_tests"
  key_links:
    - from: "tests/async_tests/state_machine_tests.rs"
      to: "src/tui/background.rs"
      via: "spawn_background_task and BackgroundEffectHandler"
      pattern: "spawn_background_task|BackgroundEffectHandler"
    - from: "tests/async_tests/state_machine_tests.rs"
      to: "src/tui/channels.rs"
      via: "EffectCommand and EffectResult channels"
      pattern: "EffectCommand::|EffectResult::"
---

<objective>
Create comprehensive state machine simulation tests for the async effect handler using dual assertion strategy. Tests verify both: (1) commands sent to the background task match expected EffectCommand variants, AND (2) final Model state correctly reflects async operation results.

Purpose: Ensure the async channel-based architecture correctly handles full
lifecycle transitions (Pending → Running → Success/Failed) with proper state
verification. Output: `state_machine_tests.rs` with complete state machine
coverage.
</objective>

<execution_context> @./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md </execution_context>

<context>
@./CLAUDE.md
@./pkgs/artifacts/CLAUDE.md
@./pkgs/artifacts/src/tui/background.rs
@./pkgs/artifacts/src/tui/channels.rs
@./pkgs/artifacts/tests/async_tests/mod.rs
@./pkgs/artifacts/tests/async_tests/background_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Create state_machine_tests.rs with full lifecycle tests</name>
  <files>pkgs/artifacts/tests/async_tests/state_machine_tests.rs</files>
  <action>
    Create new test file implementing dual assertion strategy for state machine testing:

    1. Define mock command tracker that records all EffectCommands sent to background
    2. Create test helpers for building test configurations with known artifacts
    3. Implement tests for each state transition:
       - test_check_serialization_flow: Pending → CheckRunning → CheckComplete → (NeedsGen → Pending or UpToDate)
       - test_generator_flow: Pending → Generating → GeneratorFinished → (Success → PendingSerialization or Failed → Failed)
       - test_serialize_flow: PendingSerialization → Serializing → SerializeFinished → Done
       - test_shared_artifact_flow: Full multi-target shared artifact lifecycle

    4. Each test must:
       - Track all EffectCommands sent (via mock channel wrapper)
       - Verify commands match expected variants in order
       - Process results through update() to get final Model state
       - Assert final Model reflects expected terminal state

    5. Use #[serial] for all tests to prevent shared state conflicts
    6. Use tokio::time::pause() for deterministic timing

    Pattern: Send command → Verify command variant → Receive result → Apply to model → Verify final state

</action>
  <verify>
    Run `cargo test --test tests state_machine` and verify all tests pass
  </verify>
  <done>
    state_machine_tests.rs exists with minimum 6 test functions covering:
    - Check serialization flow with dual assertions
    - Generator flow with success path
    - Generator flow with failure path
    - Serialize flow
    - Shared artifact multi-target flow
    - Batch effect processing
    All tests use #[serial] and pass with cargo test
  </done>
</task>

<task type="auto">
  <name>Add state_machine_tests to async_tests module</name>
  <files>pkgs/artifacts/tests/async_tests/mod.rs</files>
  <action>
    Add `mod state_machine_tests;` to the existing module file alongside existing modules.
    Keep alphabetical ordering if present, or append at end.
  </action>
  <verify>
    Run `cargo check --tests` to verify module compiles
  </verify>
  <done>
    mod.rs includes state_machine_tests module declaration and compiles without errors
  </done>
</task>

<task type="auto">
  <name>Verify 80% coverage for async channel components</name>
  <files>Cargo.toml</files>
  <action>
    Check if cargo-tarpaulin or similar coverage tool is configured. If not, document coverage verification approach:

    1. Run `cargo test --lib -- async` to run async tests
    2. Count lines covered in key files:
       - src/tui/channels.rs (all EffectCommand/EffectResult variants)
       - src/tui/background.rs (BackgroundEffectHandler methods)
       - src/tui/runtime.rs (effect_to_command, result_to_message)

    3. Verify critical paths are covered:
       - All EffectCommand variants sent and received
       - All EffectResult variants processed
       - Background task spawn/shutdown
       - Channel error handling (disconnect scenarios)

    Document coverage findings in test comments if gaps exist.

</action>
  <verify>
    Review test file to confirm all major code paths have corresponding tests
  </verify>
  <done>
    Coverage assessment complete with all critical paths verified:
    - CheckSerialization, RunGenerator, Serialize commands covered
    - Success and failure result paths covered
    - Channel lifecycle (spawn, send, receive, shutdown, disconnect) covered
  </done>
</task>

</tasks>

<verification>
1. Run `cargo test --test tests async::state_machine_tests` - all tests pass
2. Verify dual assertion pattern in each test: commands tracked AND final state asserted
3. Confirm #[serial] attribute on all tests for sequential execution
4. Check that test file has minimum 250 lines with comprehensive coverage
</verification>

<success_criteria>

- state_machine_tests.rs created with 6+ test functions
- All tests pass with cargo test
- Dual assertion strategy implemented (command variant + final state)
- 80% coverage target met for state machine transitions
- Module properly integrated into async_tests/ </success_criteria>

<output>
After completion, create `.planning/phases/05-validation/05-01-SUMMARY.md`
</output>
