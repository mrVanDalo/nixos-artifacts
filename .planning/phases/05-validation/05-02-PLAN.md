---
phase: 05-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkgs/artifacts/tests/async_tests/runtime_async_tests.rs
  - pkgs/artifacts/tests/async_tests/mod.rs
autonomous: true

must_haves:
  truths:
    - "Async runtime run_async() processes events and background results concurrently"
    - "Channel disconnect scenarios are handled gracefully without panics"
    - "Timeout scenarios use tokio::time::pause() for deterministic testing"
    - "tokio::select! branches are covered: shutdown, command, channel closed"
  artifacts:
    - path: "pkgs/artifacts/tests/async_tests/runtime_async_tests.rs"
      provides: "Async runtime integration tests with channel mocking"
      min_lines: 300
    - path: "pkgs/artifacts/tests/async_tests/mod.rs"
      provides: "Module exports for runtime_async_tests"
  key_links:
    - from: "tests/async_tests/runtime_async_tests.rs"
      to: "src/tui/runtime.rs"
      via: "run_async() function and channel communication"
      pattern: "run_async|res_rx\.try_recv|cmd_tx\.send"
    - from: "tests/async_tests/runtime_async_tests.rs"
      to: "src/tui/background.rs"
      via: "spawn_background_task with mock channels"
      pattern: "spawn_background_task"
---

<objective>
Create async runtime integration tests that verify the run_async() function correctly handles the async channel-based architecture. Tests cover channel-level mocking, tokio::select! branch coverage, timeout scenarios with mock time, and graceful error handling for disconnect scenarios.

Purpose: Ensure the async runtime (run_async) properly coordinates between foreground TUI and background task via channels, with robust handling of edge cases.
Output: `runtime_async_tests.rs` with comprehensive async runtime coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./pkgs/artifacts/CLAUDE.md
@./pkgs/artifacts/src/tui/runtime.rs
@./pkgs/artifacts/src/tui/background.rs
@./pkgs/artifacts/src/tui/channels.rs
@./pkgs/artifacts/tests/async_tests/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Create runtime_async_tests.rs with async runtime tests</name>
  <files>pkgs/artifacts/tests/async_tests/runtime_async_tests.rs</files>
  <action>
    Create comprehensive async runtime tests covering:
    
    1. Channel-level mocking infrastructure:
       - MockEventSource that implements EventSource trait for testing
       - MockCommandTracker that wraps channels and records all commands
       - Test helper to create TestBackend with TestBackend for ratatui
    
    2. Core runtime tests:
       - test_run_async_processes_events: Verify run_async() handles scripted events
       - test_run_async_drains_results_before_blocking: Verify drain phase processes pending results
       - test_run_async_sends_effects_to_background: Verify effects become EffectCommands
       - test_run_async_handles_results: Verify EffectResults are converted to Msgs
    
    3. tokio::select! branch coverage tests:
       - test_select_shutdown_branch: CancellationToken.cancel() triggers shutdown
       - test_select_result_branch: res_rx.recv() processes background results
       - test_select_command_branch: cmd_tx.send() dispatches to background
       - test_select_channel_closed: else branch when channel closed
    
    4. Error handling tests:
       - test_channel_disconnect_graceful: tx_cmd dropped, rx_res returns None
       - test_background_panic_handled: Background task panic doesn't crash foreground
       - test_result_channel_disconnect: rx_res dropped, send fails gracefully
    
    5. Timeout tests with mock time:
       - test_timeout_with_paused_time: Use tokio::time::pause() and advance()
       - test_shutdown_drain_timeout: Verify graceful shutdown timeout handling
    
    6. Sequential execution with #[serial] on all tests
    
    Key testing pattern:
    - Create TestBackend, MockEventSource
    - Spawn background task with real BackgroundEffectHandler
    - Run run_async() with timeout wrapper
    - Verify final model state and tracked commands
  </action>
  <verify>
    Run `cargo test --test tests runtime_async` and verify all tests pass
  </verify>
  <done>
    runtime_async_tests.rs exists with minimum 10 test functions:
    - 4 core runtime tests (events, draining, sending, receiving)
    - 4 select! branch tests (shutdown, result, command, closed)
    - 3 error handling tests (disconnect, panic, result channel)
    - 2 timeout tests (paused time, shutdown drain)
    All tests use #[serial] and pass with cargo test
  </done>
</task>

<task type="auto">
  <name>Add runtime_async_tests to async_tests module</name>
  <files>pkgs/artifacts/tests/async_tests/mod.rs</files>
  <action>
    Add `mod runtime_async_tests;` to the module file alongside existing modules.
    Keep alphabetical ordering: background_tests, channel_tests, runtime_async_tests, select_tests, shutdown_tests, state_machine_tests
  </action>
  <verify>
    Run `cargo check --tests` to verify module compiles
  </verify>
  <done>
    mod.rs includes runtime_async_tests module declaration and compiles without errors
  </done>
</task>

<task type="auto">
  <name>Verify critical error scenarios coverage</name>
  <files>pkgs/artifacts/tests/async_tests/runtime_async_tests.rs</files>
  <action>
    Ensure tests cover the two critical failure modes from research:
    
    1. Channel disconnect (tx_cmd dropped while rx_res active):
       - Test: Drop tx_cmd after sending some commands
       - Verify: rx_res.recv() returns None, no panic
       - Assert: Foreground handles gracefully
    
    2. Timeout scenarios:
       - Test: Use tokio::time::pause() and manual advancement
       - Verify: timeout triggers after specified duration
       - Assert: Graceful handling without data loss
    
    Also verify background loop coverage:
    - tokio::select! main loop branches
    - In-flight command completion during shutdown
    - CancellationToken propagation
  </action>
  <verify>
    Review test file to confirm both critical failure modes have dedicated tests
  </verify>
  <done>
    Critical error scenarios covered:
    - Channel disconnect test exists and passes
    - Timeout with mock time test exists and passes
    - Background loop branches all have test coverage
  </done>
</task>

</tasks>

<verification>
1. Run `cargo test --test tests async::runtime_async_tests` - all tests pass
2. Verify all #[serial] attributes present for sequential execution
3. Check that tokio::time::pause() is used in timeout tests (not real time)
4. Confirm select! branches have dedicated tests
5. Verify channel disconnect and timeout scenarios are covered
</verification>

<success_criteria>
- runtime_async_tests.rs created with 10+ test functions
- All tests pass with cargo test
- Channel-level mocking implemented for direct testing
- tokio::select! branches covered (shutdown, result, command, closed)
- Critical error scenarios tested: channel disconnect and timeout
- All tests use #[serial] for sequential execution
- Module properly integrated into async_tests/
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation/05-02-SUMMARY.md`
</output>
